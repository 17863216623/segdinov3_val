# 关键参数配置说明

本文档列出项目的关键架构参数配置。

---

## 1. DINOv3 Block 抽取下标

### 配置位置
- **文件**: `dpt.py` 第68-71行
- **代码**:
```python
self.intermediate_layer_idx = {
    'small': [2, 5, 8, 11],  # DINOv3-Small
    'base':  [2, 5, 8, 11],  # DINOv3-Base
}
```

### 参数说明
- **抽取的Block下标**: `[2, 5, 8, 11]`
- **含义**: 
  - Layer 2: 第3个Transformer块（索引从0开始）
  - Layer 5: 第6个Transformer块
  - Layer 8: 第9个Transformer块
  - Layer 11: 第12个Transformer块（最后一层）
- **用途**: 多尺度特征提取，用于DPT Head融合

### 对应关系
| Block索引 | 实际层数 | 特征维度 (Small) | 特征维度 (Base) |
|-----------|----------|------------------|-----------------|
| 2 | 第3层 | 384 | 768 |
| 5 | 第6层 | 384 | 768 |
| 8 | 第9层 | 384 | 768 |
| 11 | 第12层 | 384 | 768 |

---

## 2. Token/Query 数量

### 2.1 Token 数量

**配置位置**: `mamba_decoder.py` 第30-45行 (`ImageTokenEncoder`)

**Token生成过程**:
```python
# 输入: image [B×3×H×W], prob [B×1×H×W]
x = cat([img, prob])  # [B×4×H×W]
feat = enc(x)         # [B×192×H/4×W/4] (经过2次下采样)
tok = feat.flatten(2).permute(0,2,1)  # [B×L×192]
```

**Token数量**:
- **L = (H/4) × (W/4)** (动态计算)
- **示例**: 
  - 输入 384×384 → Token数量 = 96×96 = 9216
  - 输入 256×256 → Token数量 = 64×64 = 4096

**Token维度**: 192 (token_dim)

### 2.2 Query 数量

**配置位置**: 
- **默认值**: `mamba_decoder.py` 第70行 (`num_queries=12`)
- **训练脚本**: `train_segdinov3.py` 第316行 (`--mamba_num_queries 12`)
- **激进优化**: `train_aggressive.sh` 第24行 (`MAMBA_NUM_QUERIES=12`)

**参数值**:
- **Query数量**: **12** (固定)
- **Query维度**: 192 (token_dim)

**代码实现**:
```python
# mamba_decoder.py 第73行
self.queries = nn.Parameter(torch.randn(1, num_queries, token_dim) * 0.02)
# 形状: [1, 12, 192]
```

**用途**: 可学习的查询向量，用于与token交互生成掩码

---

## 3. GroupSSM 分组数

**配置位置**:
- **默认值**: `mamba_decoder.py` 第48行 (`groups=4`)
- **训练脚本**: `train_segdinov3.py` 第319行 (`--mamba_groups 4`)
- **激进优化**: `train_aggressive.sh` 第27行 (`MAMBA_GROUPS=4`)

**参数值**:
- **GroupSSM分组数**: **4**

**代码实现**:
```python
# mamba_decoder.py 第47-48行
class GroupSSM1D(nn.Module):
    def __init__(self, dim, kernel_size=9, groups=4, expansion=2):
        # groups=4 用于深度可分离卷积的分组
```

**相关参数**:
- **kernel_size**: 9 (卷积核大小)
- **expansion**: 2 (FFN扩展倍数)

**用途**: 分组卷积，减少参数量和计算量

---

## 4. UGBD 迭代步数 T

**配置位置**:
- **默认值**: `ugbd_refiner.py` 第47行 (`steps=3`)
- **训练脚本默认**: `train_segdinov3.py` 第325行 (`--ugbd_steps 3`)
- **激进优化**: `train_aggressive.sh` 第47行 (`UGBD_STEPS=4`)

**参数值**:
- **默认迭代步数**: **3**
- **激进优化迭代步数**: **4**

**代码实现**:
```python
# ugbd_refiner.py 第59-73行
def forward(self, image, logits):
    # ... 特征提取和融合 ...
    h = None
    for _ in range(self.steps):  # steps=3 或 4
        h = self.gru(x, h)
    res = self.out(h)
    return logits + res
```

**迭代公式**:
```
h⁰ = 0
For t = 1 to T (T=3或4):
    z^t, r^t = σ(Conv_zr([x, h^{t-1}]))
    h̃^t = tanh(Conv_h([x, r^t ⊙ h^{t-1}]))
    h^t = (1 - z^t) ⊙ h^{t-1} + z^t ⊙ h̃^t
Δ = Out(h^T)
refined_logits = logits + Δ
```

**参数选择**:
- **steps=3**: 平衡效果和速度（默认）
- **steps=4**: 更精细的细化，但计算量增加（激进优化）

---

## 5. SDF 融合形式与权重

### 5.1 融合形式

**配置位置**: `sdf_head.py` 第117-124行

**融合公式**:
```python
refined_logits = logits - fuse_lambda * sdf
```

**融合方式**: **残差减法**
- 从logits中减去加权后的SDF预测
- SDF值越大（距离边界越远），logits减小越多
- SDF值越小（接近边界），logits减小越少

**数学表达**:
```
refined_logits = logits - λ × SDF_pred

其中:
  λ = fuse_lambda (融合权重)
  SDF_pred: 预测的有符号距离场
```

### 5.2 融合权重 (fuse_lambda)

**配置位置**:
- **默认值**: `sdf_head.py` 第100行 (`fuse_lambda=0.5`)
- **训练脚本默认**: `train_segdinov3.py` 第331行 (`--sdf_lambda 0.5`)
- **激进优化**: `train_aggressive.sh` 第60行 (`SDF_LAMBDA=0.35`)

**参数值**:
- **默认权重**: **0.5**
- **激进优化权重**: **0.35**

**权重影响**:
- **λ越大**: SDF对logits的影响越大，边界校正更强
- **λ越小**: SDF对logits的影响越小，保持原始预测更多
- **激进优化降低到0.35**: 减少SDF的干扰，更温和的校正

### 5.3 SDF相关损失权重

**配置位置**: `train_aggressive.sh` 第61-64行

**损失权重配置**:
```bash
W_SDF=0.15      # SDF L1损失权重
W_EIK=0.08      # Eikonal损失权重（梯度模长约束）
W_CURV=0.04     # 曲率损失权重（平滑约束）
SDF_SCALE=0.7   # SDF GT缩放因子
```

**损失公式**:
```
L_SDF = w_sdf × L1(SDF_pred, SDF_gt)
      + w_eik × Eikonal(SDF_pred)
      + w_curv × Curvature(SDF_pred)
```

---

## 参数总结表

| 参数类别 | 参数名 | 默认值 | 激进优化值 | 说明 |
|---------|--------|--------|-----------|------|
| **Block下标** | `intermediate_layer_idx` | `[2,5,8,11]` | `[2,5,8,11]` | DINOv3特征提取层 |
| **Token数量** | `L` | `(H/4)×(W/4)` | `(H/4)×(W/4)` | 动态计算 |
| **Query数量** | `num_queries` | `12` | `12` | 固定 |
| **GroupSSM分组** | `groups` | `4` | `4` | 分组卷积 |
| **UGBD迭代步** | `ugbd_steps` | `3` | `4` | ConvGRU迭代次数 |
| **SDF融合权重** | `sdf_lambda` | `0.5` | `0.35` | SDF残差权重 |
| **SDF L1权重** | `w_sdf` | `0.10` | `0.15` | SDF预测损失 |
| **Eikonal权重** | `w_eik` | `0.05` | `0.08` | 梯度模长约束 |
| **Curvature权重** | `w_curv` | `0.02` | `0.04` | 曲率平滑约束 |
| **SDF缩放** | `sdf_scale` | `1.0` | `0.7` | GT SDF缩放因子 |

---

## 代码位置索引

### Block下标
- **定义**: `dpt.py:68-71`
- **使用**: `dpt.py:83-84`

### Token/Query
- **Token编码**: `mamba_decoder.py:30-45` (`ImageTokenEncoder`)
- **Query定义**: `mamba_decoder.py:73`
- **Query数量参数**: `train_segdinov3.py:316`

### GroupSSM分组
- **定义**: `mamba_decoder.py:48` (`GroupSSM1D.__init__`)
- **使用**: `mamba_decoder.py:74`
- **参数**: `train_segdinov3.py:319`

### UGBD迭代步
- **定义**: `ugbd_refiner.py:47` (`UGBDRefiner.__init__`)
- **使用**: `ugbd_refiner.py:70-71`
- **参数**: `train_segdinov3.py:325`

### SDF融合
- **定义**: `sdf_head.py:100` (`SDFHead.__init__`)
- **使用**: `sdf_head.py:123`
- **参数**: `train_segdinov3.py:331`

---

**文档版本**: v1.0  
**最后更新**: 2025年

