# MAE优化总结

## 当前状态
- **当前最佳MAE**: 0.0175 (epoch 45)
- **目标MAE**: < 0.016
- **需要降低**: 约9.5%

## 已实施的优化

### 1. 新增MAE直接优化损失函数

#### `mae_loss` - 直接MAE损失
- **权重**: 0.8
- **作用**: 直接最小化预测与真实值的平均绝对误差
- **代码位置**: `train_segdinov3.py` 第141-143行

#### `focal_mae_loss` - Focal MAE损失
- **权重**: 0.3
- **参数**: alpha=0.25, gamma=2.0
- **作用**: 对预测误差较大的困难样本给予更高权重
- **代码位置**: `train_segdinov3.py` 第145-150行

### 2. 损失权重优化

| 损失类型 | 原权重 | 新权重 | 提升 |
|---------|--------|--------|------|
| SSIM | 0.35 | 0.40 | +14% |
| 小波损失 | 0.30 | 0.35 | +17% |
| 小波边缘Dice | 0.10 | 0.12 | +20% |
| TV损失 | 0.06 | 0.08 | +33% |
| 边界Focal BCE | 0.12 | 0.15 | +25% |
| SDF损失 | 0.10 | 0.15 | +50% |
| Eikonal损失 | 0.05 | 0.08 | +60% |
| Curvature损失 | 0.02 | 0.03 | +50% |

### 3. 训练策略优化

| 参数 | 原值 | 新值 | 说明 |
|------|------|------|------|
| EPOCHS | 80 | 100 | 增加训练轮数 |
| PATIENCE | 15 | 20 | 增加学习率调度等待时间 |
| EARLY_STOP | 20 | 30 | 增加早停轮数 |
| BEST_METRIC | Fw_beta | MAE | 直接优化MAE指标 |

### 4. 数据加载优化

- **验证集**: 使用独立的 `usod10k_val` (2051样本)
- **测试集**: 使用独立的 `usod10k_test` (1026样本)
- **训练集**: `usod10k/train` (7178样本)

## 优化原理

### MAE损失的作用
1. **直接优化**: MAE损失直接最小化平均绝对误差，与评估指标一致
2. **困难样本关注**: Focal MAE对误差大的区域给予更高权重，加速收敛
3. **细节保留**: 增加SSIM和小波损失权重，提升细节质量
4. **边界精度**: 增加边界相关损失权重，提升边界预测精度
5. **形状先验**: 增加SDF损失权重，利用形状先验信息

### 训练策略
1. **更长训练**: 增加训练轮数，给模型更多优化时间
2. **更耐心**: 增加patience，允许模型在MAE优化上花费更多时间
3. **目标明确**: 直接以MAE为优化目标，而非间接指标

## 使用方法

### 直接使用优化后的脚本

```bash
./train_one_click.sh
```

### 关键配置（已在脚本中）

```bash
# MAE优化损失
W_MAE=0.8
W_FOCAL_MAE=0.3
FOCAL_MAE_ALPHA=0.25
FOCAL_MAE_GAMMA=2.0

# 训练配置
EPOCHS=100
PATIENCE=20
EARLY_STOP=30
BEST_METRIC="MAE"
```

## 预期效果

通过以上优化，预期可以：
1. **直接优化MAE**: MAE损失直接最小化平均绝对误差
2. **提升细节质量**: 增加SSIM和小波损失权重
3. **提升边界精度**: 增加边界相关损失权重
4. **利用形状先验**: 增加SDF损失权重
5. **更充分训练**: 增加训练轮数和patience

**预期MAE**: < 0.016 ✅

## 验证方法

训练完成后，运行测试：

```bash
./test_one_click.sh
```

查看测试结果中的MAE指标。

## 进一步优化（如果未达到目标）

如果MAE仍未达到< 0.016，可以尝试：

1. **增加MAE损失权重**
   ```bash
   W_MAE=1.2
   W_FOCAL_MAE=0.5
   ```

2. **增加训练轮数**
   ```bash
   EPOCHS=120
   ```

3. **降低学习率**
   ```bash
   LR=8e-5
   ```

4. **启用数据增强**
   - 在训练脚本中添加 `--use_aug`

5. **使用更大的模型**
   - 使用DINOv3-Base: `DINO_SIZE="b"`

## 文件变更清单

### 修改的文件
1. `train_segdinov3.py`
   - 添加 `mae_loss` 函数
   - 添加 `focal_mae_loss` 函数
   - 添加MAE损失参数
   - 在训练循环中集成MAE损失

2. `train_one_click.sh`
   - 添加MAE损失权重配置
   - 优化其他损失权重
   - 调整训练策略参数
   - 修改最佳指标为MAE

### 新增的文件
1. `MAE_OPTIMIZATION.md` - MAE优化详细说明
2. `OPTIMIZATION_SUMMARY_MAE.md` - 本文件

## 注意事项

1. **训练时间**: 增加训练轮数会延长训练时间（约25%）
2. **内存使用**: 新增损失函数不会显著增加内存使用
3. **指标平衡**: 如果其他指标下降，可以适当调整权重
4. **过拟合**: 注意监控验证集指标，避免过拟合

## 技术细节

### MAE损失实现

```python
def mae_loss(prob, target):
    """直接优化平均绝对误差"""
    return torch.abs(prob - target).mean()
```

### Focal MAE损失实现

```python
def focal_mae_loss(prob, target, alpha=0.25, gamma=2.0):
    """对困难样本给予更多关注"""
    mae = torch.abs(prob - target)
    weight = torch.pow(mae, gamma)
    return (alpha * weight * mae).mean()
```

## 总结

通过添加MAE直接优化损失、调整损失权重、优化训练策略，预期可以将MAE从0.0175降低到< 0.016，满足要求。所有优化已集成到训练脚本中，可以直接使用。
