# 核心技术细节说明

本文档详细解释三个核心技术细节：
1. DINOv3 到 Decoder 的特征接口
2. Wavelet 的使用方式
3. UGBD 细化过程

---

## 1. DINOv3 到 Decoder 的特征接口

### 1.1 特征提取层选择

**提取的层索引**:
```python
# dpt.py 第68-71行
self.intermediate_layer_idx = {
    'small': [2, 5, 8, 11],  # DINOv3-Small 的4个Transformer块
    'base':  [2, 5, 8, 11],  # DINOv3-Base 的4个Transformer块
}
```

**对应关系**:
- **Layer 2**: 第3个Transformer块（索引从0开始，实际是第3层）
- **Layer 5**: 第6个Transformer块
- **Layer 8**: 第9个Transformer块
- **Layer 11**: 第12个Transformer块（最后一层）

**为什么选择这4层？**
- 多尺度特征：从浅层到深层，捕获不同抽象级别的特征
- 标准DPT设计：遵循DPT（Dense Prediction Transformer）的多尺度特征融合策略
- 计算效率：避免提取所有层，减少计算开销

### 1.2 Backbone 冻结策略

**代码实现**:
```python
# dpt.py 第77-79行
def lock_backbone(self):
    for p in self.backbone.parameters():
        p.requires_grad = False
```

**实际使用情况**:
- **训练脚本中**: 虽然定义了`lock_backbone()`方法，但在`train_segdinov3.py`中**没有显式调用**
- **参数更新**: 优化器包含`backbone.parameters()`，但通过`filter(lambda p: p.requires_grad, params)`过滤
- **实际行为**: 
  - 如果backbone参数默认`requires_grad=True`，则会被更新
  - 如果backbone参数默认`requires_grad=False`（DINOv3预训练模型通常如此），则不会被更新
  - **建议**: 显式调用`backbone.lock_backbone()`确保冻结

**冻结的好处**:
- 保持预训练特征表示
- 减少可训练参数
- 加快训练速度
- 降低过拟合风险

### 1.3 维度对齐流程

#### 步骤1: DINOv3特征提取

```python
# dpt.py 第81-86行
def forward(self, x):
    patch_h, patch_w = x.shape[-2] // 16, x.shape[-1] // 16  # patch尺寸
    features = self.backbone.get_intermediate_layers(
        x, n = self.intermediate_layer_idx[self.encoder_size]
    )
    # features: List[4个Tensor], 每个是 [B, N_patches, embed_dim]
    # 其中 N_patches = patch_h × patch_w = (H/16) × (W/16)
```

**输出格式**:
- 4个特征张量，每个形状为 `[B, N_patches, embed_dim]`
- `N_patches = (H/16) × (W/16)` (因为patch size=16)
- `embed_dim`: 
  - DINOv3-Small: 384
  - DINOv3-Base: 768

#### 步骤2: 特征重塑与投影

```python
# dpt.py 第36-41行 (DPTHead.forward)
def forward(self, out_features, patch_h, patch_w):
    out = []
    for i, x in enumerate(out_features):
        # 1. 重塑为空间特征图
        x = x.permute(0, 2, 1).reshape((x.shape[0], x.shape[-1], patch_h, patch_w))
        # 形状: [B, embed_dim, patch_h, patch_w]
        
        # 2. 1×1投影到统一维度
        x = self.projects[i](x)
        # 形状: [B, out_channels[i], patch_h, patch_w]
        out.append(x)
```

**投影维度配置**:
```python
# dpt.py 第62行 (DPT初始化)
out_channels=[96, 192, 384, 768]  # DINOv3-Small默认
# 或
out_channels=[256, 512, 1024, 768]  # DINOv3-Base默认
```

**投影矩阵**:
- `self.projects`: `nn.ModuleList`，包含4个`nn.Conv2d(embed_dim, out_channels[i], 1)`
- 每个投影是1×1卷积，不改变空间尺寸

#### 步骤3: 特征融合与统一

```python
# dpt.py 第43-53行
layer_1, layer_2, layer_3, layer_4 = out
# 每个形状: [B, out_channels[i], patch_h_i, patch_w_i]

# 3. 通过残差网络统一特征
layer_1_rn = self.scratch.layer1_rn(layer_1)  # [B, features, patch_h, patch_w]
layer_2_rn = self.scratch.layer2_rn(layer_2)  # [B, features, patch_h/2, patch_w/2]
layer_3_rn = self.scratch.layer3_rn(layer_3)  # [B, features, patch_h/4, patch_w/4]
layer_4_rn = self.scratch.layer4_rn(layer_4)  # [B, features, patch_h/8, patch_w/8]

# 4. 上采样到统一尺寸
target_hw = layer_1_rn.shape[-2:]  # 最大尺寸
layer_2_up = F.interpolate(layer_2_rn, size=target_hw, mode="bilinear", align_corners=True)
layer_3_up = F.interpolate(layer_3_rn, size=target_hw, mode="bilinear", align_corners=True)
layer_4_up = F.interpolate(layer_4_rn, size=target_hw, mode="bilinear", align_corners=True)

# 5. 拼接融合
fused = torch.cat([layer_1_rn, layer_2_up, layer_3_up, layer_4_up], dim=1)
# 形状: [B, features×4, patch_h, patch_w]

# 6. 输出logits
out = self.scratch.output_conv(fused)  # [B, 1, patch_h, patch_w]
```

**统一维度** (`blocks.py`):
```python
# blocks.py 第9-12行
scratch.layer1_rn = nn.Conv2d(in_shape[0], out_shape, 3, 1, 1, ...)  # 96→features
scratch.layer2_rn = nn.Conv2d(in_shape[1], out_shape, 3, 1, 1, ...)  # 192→features
scratch.layer3_rn = nn.Conv2d(in_shape[2], out_shape, 3, 1, 1, ...)  # 384→features
scratch.layer4_rn = nn.Conv2d(in_shape[3], out_shape, 3, 1, 1, ...)  # 768→features
```

**最终输出**:
```python
# dpt.py 第87行
out = F.interpolate(out, (patch_h * 16, patch_w * 16), mode='bilinear', align_corners=True)
# 形状: [B, 1, H, W]  (恢复到原始图像尺寸)
```

### 1.4 完整流程图

```
输入图像 [B, 3, H, W]
    ↓
DINOv3 Backbone
    ↓
get_intermediate_layers([2,5,8,11])
    ↓
4个特征 [B, N_p, embed_dim] × 4
    ↓
重塑: permute + reshape
    ↓
[B, embed_dim, patch_h, patch_w] × 4
    ↓
1×1投影 (projects)
    ↓
[B, out_ch[i], patch_h_i, patch_w_i] × 4
    ↓
残差网络统一 (layer*_rn)
    ↓
[B, features, patch_h_i, patch_w_i] × 4
    ↓
上采样到统一尺寸
    ↓
[B, features, patch_h, patch_w] × 4
    ↓
拼接: cat(dim=1)
    ↓
[B, features×4, patch_h, patch_w]
    ↓
输出卷积
    ↓
初始logits [B, 1, patch_h, patch_w]
    ↓
上采样到原图尺寸
    ↓
最终logits [B, 1, H, W]
```

### 1.5 维度总结表

| 阶段 | Layer 2 | Layer 5 | Layer 8 | Layer 11 |
|------|---------|---------|---------|----------|
| **原始特征** | [B, N, 384] | [B, N, 384] | [B, N, 384] | [B, N, 384] |
| **重塑后** | [B, 384, H/16, W/16] | [B, 384, H/32, W/32] | [B, 384, H/64, W/64] | [B, 384, H/128, W/128] |
| **投影后** | [B, 96, H/16, W/16] | [B, 192, H/32, W/32] | [B, 384, H/64, W/64] | [B, 768, H/128, W/128] |
| **统一后** | [B, features, H/16, W/16] | [B, features, H/32, W/32] | [B, features, H/64, W/64] | [B, features, H/128, W/128] |
| **上采样后** | [B, features, H/16, W/16] | [B, features, H/16, W/16] | [B, features, H/16, W/16] | [B, features, H/16, W/16] |
| **融合后** | [B, features×4, H/16, W/16] | | | |
| **最终输出** | [B, 1, H, W] | | | |

**注意**: 上表假设DINOv3-Small，实际空间尺寸可能因模型配置而异。

---

## 2. Wavelet 的使用方式

Wavelet在项目中有**两种用途**：
1. **作为损失函数**：在训练时约束预测结果
2. **作为边缘引导**：在UGBD和SDF中提取边缘特征

### 2.1 Wavelet 作为损失函数

#### 2.1.1 小波分解实现

**Haar小波核**:
```python
# losses_wavelet.py 第8-16行
def _haar_filters(device, dtype):
    h0 = [1.0, 1.0] / √2  # 低通
    h1 = [1.0,-1.0] / √2  # 高通
    LL = h0 ⊗ h0  # 低-低（近似）
    LH = h0 ⊗ h1  # 低-高（水平细节）
    HL = h1 ⊗ h0  # 高-低（垂直细节）
    HH = h1 ⊗ h1  # 高-高（对角细节）
    return [LL, LH, HL, HH]  # 4个2×2核
```

**多尺度分解**:
```python
# losses_wavelet.py 第35-41行
def dwt2_haar_levels(x, levels=3):
    out, cur = [], x
    for level in range(levels):
        LL, (LH, HL, HH) = dwt2_haar(cur)
        out.append((LL, (LH, HL, HH)))
        cur = LL  # 下一层对LL继续分解
    return out
```

**分解过程**:
```
Level 0: [B,1,H,W]
    ↓ dwt2_haar
    LL: [B,1,H/2,W/2], (LH,HL,HH): [B,1,H/2,W/2] × 3

Level 1: LL from Level 0
    ↓ dwt2_haar
    LL: [B,1,H/4,W/4], (LH,HL,HH): [B,1,H/4,W/4] × 3

Level 2: LL from Level 1
    ↓ dwt2_haar
    LL: [B,1,H/8,W/8], (LH,HL,HH): [B,1,H/8,W/8] × 3
```

#### 2.1.2 Wavelet Detail Loss (WDL)

**公式**:
```
L_wdl = (1/L) × Σ_l ||P_mag^l - G_mag^l||_1

其中:
  P_mag^l = |PLH^l| + |PHL^l| + |PHH^l|  (预测的细节幅度)
  G_mag^l = |GLH^l| + |GHL^l| + |GHH^l|  (GT的细节幅度)
  L = levels (默认3)
```

**代码实现**:
```python
# losses_wavelet.py 第44-52行
def wavelet_detail_loss(prob, gt, levels=3, eps=1e-6):
    plist = dwt2_haar_levels(prob, levels=levels)
    glist = dwt2_haar_levels(gt,   levels=levels)
    losses = []
    for (PLL, (PLH, PHL, PHH)), (GLL, (GLH, GHL, GHH)) in zip(plist, glist):
        Pmag = torch.abs(PLH) + torch.abs(PHL) + torch.abs(PHH)
        Gmag = torch.abs(GLH) + torch.abs(GHL) + torch.abs(GHH)
        losses.append(F.l1_loss(Pmag, Gmag, reduction="mean"))
    return sum(losses) / max(1, len(losses))
```

**作用**: 保持预测结果的高频细节信息

#### 2.1.3 Wavelet Edge Dice Loss (WED)

**公式**:
```
L_wed = (1/L) × Σ_l (1 - Dice(P_norm^l, G_norm^l))

其中:
  P_norm^l = normalize(P_mag^l)  (归一化到[0,1])
  G_norm^l = normalize(G_mag^l)
  Dice(P, G) = 2|P∩G| / (|P| + |G|)
```

**代码实现**:
```python
# losses_wavelet.py 第55-68行
def wavelet_edge_dice(prob, gt, levels=3, eps=1e-6):
    plist = dwt2_haar_levels(prob, levels=levels)
    glist = dwt2_haar_levels(gt,   levels=levels)
    losses = []
    for (PLL, (PLH, PHL, PHH)), (GLL, (GLH, GHL, GHH)) in zip(plist, glist):
        Pmag = torch.abs(PLH) + torch.abs(PHL) + torch.abs(PHH)
        Gmag = torch.abs(GLH) + torch.abs(GHL) + torch.abs(GHH)
        # 归一化
        Pn = (Pmag - Pmag.min()) / (Pmag.max() - Pmag.min() + eps)
        Gn = (Gmag - Gmag.min()) / (Gmag.max() - Gmag.min() + eps)
        # Dice计算
        inter = (Pn*Gn).sum(dim=(1,2,3))
        den   = Pn.sum(dim=(1,2,3)) + Gn.sum(dim=(1,2,3)) + eps
        dice  = (2*inter + eps)/den
        losses.append(1 - dice.mean())
    return sum(losses) / max(1, len(losses))
```

**作用**: 在边缘区域保持高重叠度

#### 2.1.4 TV-L1 Loss

**公式**:
```
L_tv = weight × (||∂P/∂x||_1 + ||∂P/∂y||_1)

其中:
  ||∂P/∂x||_1 = mean(|P[:,:,1:] - P[:,:,:-1]|)
  ||∂P/∂y||_1 = mean(|P[:,1:,:] - P[:,:-1,:]|)
```

**代码实现**:
```python
# losses_wavelet.py 第71-74行
def tv_l1_loss(prob, weight=1.0):
    dx = torch.abs(prob[:,:,:,1:] - prob[:,:,:,:-1]).mean()
    dy = torch.abs(prob[:,:,1:,:] - prob[:,:,:-1,:]).mean()
    return weight * (dx + dy)
```

**作用**: 总变分正则化，平滑预测结果，减少噪声

#### 2.1.5 Boundary Focal BCE Loss

**公式**:
```
L_bfce = mean(w × BCE(logits, gt))

其中:
  p = sigmoid(logits)
  u = 4 × p × (1-p)  (不确定性)
  band = dilate(gt) - erode(gt)  (边界带)
  w = 1 + λ × band × u^γ  (权重，γ=2.0)
```

**代码实现**:
```python
# losses_wavelet.py 第85-91行
def boundary_focal_bce_loss(logits, gt, lam=1.0, gamma=2.0):
    p  = torch.sigmoid(logits).clamp(1e-6, 1-1e-6)
    bce = F.binary_cross_entropy_with_logits(logits, gt, reduction="none")
    band = _morph_band(gt)  # 形态学边界带
    u = 4.0 * p * (1.0 - p)  # 不确定性
    w = 1.0 + lam * band * (u ** gamma)  # 边界+不确定性加权
    return (w * bce).mean()
```

**作用**: 在边界区域和高不确定性区域加强损失权重

### 2.2 Wavelet 作为边缘引导

#### 2.2.1 Wavelet-like Edge Extraction

**实现** (`ugbd_refiner.py` 第6-22行):
```python
def wavelet_like_edges(x: torch.Tensor) -> torch.Tensor:
    # x: B×3×H×W (图像)
    
    # 3个固定高通核（近似小波高频）
    kx = [[-1,-1,-1],    # 水平边缘
          [ 2, 2, 2],
          [-1,-1,-1]]
    
    ky = kx^T            # 垂直边缘（转置）
    
    kd = [[ 2,-1,-1],    # 对角边缘
          [-1, 2,-1],
          [-1,-1, 2]]
    
    # 分组卷积（每个通道独立）
    ex = conv2d(x, kx, groups=C)  # 水平响应
    ey = conv2d(x, ky, groups=C)  # 垂直响应
    ed = conv2d(x, kd, groups=C)  # 对角响应
    
    # 融合: 欧几里得范数
    e = sqrt(ex² + ey² + ed² + ε)
    
    # 跨通道平均
    e = mean(e, dim=1, keepdim=True)  # B×1×H×W
    
    # 归一化到[0,1]
    e = e / (e.max() + ε)
    
    return e
```

**特点**:
- **固定核**: 不使用可学习参数，计算快速
- **多方向**: 捕获水平、垂直、对角边缘
- **归一化**: 输出在[0,1]范围

#### 2.2.2 在UGBD中的使用

```python
# ugbd_refiner.py 第59-73行
def forward(self, image, logits):
    prob = torch.sigmoid(logits)
    uncert = 4.0 * prob * (1.0 - prob)
    
    # 提取边缘特征（作为输入）
    img_feat0 = F.relu(self.in_conv(image))  # B×8×H×W
    edge = wavelet_like_edges(image)          # B×1×H×W
    
    # 特征融合
    x = torch.cat([img_feat0, prob, edge, uncert], dim=1)  # B×11×H×W
    x = self.fuse(x)  # B×mid_ch×H×W
    
    # ConvGRU迭代细化
    h = None
    for _ in range(self.steps):
        h = self.gru(x, h)
    
    res = self.out(h)  # B×1×H×W
    return logits + res
```

**作用**: 边缘信息引导细化过程，关注边界区域

#### 2.2.3 在SDF Head中的使用

```python
# sdf_head.py 第117-124行
def forward(self, image, logits):
    prob = torch.sigmoid(logits)
    edge = wavelet_like_edges(image)  # B×1×H×W
    
    # 特征拼接
    x = torch.cat([image, prob, edge], dim=1)  # B×5×H×W
    
    feat = self.enc(x)  # B×mid_ch×H×W
    sdf = self.to_sdf(feat)  # B×1×H×W
    
    refined = logits - self.fuse_lambda * sdf
    return refined, sdf
```

**作用**: 边缘信息帮助SDF预测，特别是边界附近的距离场

### 2.3 Wavelet使用总结

| 用途 | 函数/模块 | 输入 | 输出 | 作用 |
|------|----------|------|------|------|
| **损失-WDL** | `wavelet_detail_loss` | prob, gt | scalar | 保持高频细节 |
| **损失-WED** | `wavelet_edge_dice` | prob, gt | scalar | 边缘区域重叠度 |
| **损失-TV** | `tv_l1_loss` | prob | scalar | 平滑正则化 |
| **损失-BFCE** | `boundary_focal_bce_loss` | logits, gt | scalar | 边界聚焦 |
| **边缘引导** | `wavelet_like_edges` | image | edge map | 提取边缘特征 |
| **UGBD输入** | UGBD.forward | image | edge | 引导细化 |
| **SDF输入** | SDFHead.forward | image | edge | 辅助SDF预测 |

---

## 3. UGBD 细化过程

### 3.1 UGBD 概述

**UGBD** = **U**ncertainty-**G**uided **B**oundary **D**iffusion Refiner

**核心思想**:
- 使用**不确定性**识别需要细化的区域
- 使用**边缘信息**引导边界优化
- 通过**迭代细化**逐步改善预测

### 3.2 完整前向流程

#### 步骤1: 计算不确定性

```python
# ugbd_refiner.py 第61-62行
prob = torch.sigmoid(logits)  # B×1×H×W, [0,1]
uncert = 4.0 * prob * (1.0 - prob)  # B×1×H×W, [0,1]
```

**不确定性公式**:
```
uncertainty = 4 × p × (1-p)

其中:
  p = sigmoid(logits)
  
性质:
  - p=0.5时，uncertainty最大 (=1.0)
  - p→0或p→1时，uncertainty→0
  - 峰值在边界区域（预测不确信的地方）
```

**可视化**:
```
prob:     [0.0, 0.3, 0.5, 0.7, 1.0]
uncert:   [0.0, 0.84, 1.0, 0.84, 0.0]
          ↑              ↑              ↑
        背景          边界区域        前景
```

#### 步骤2: 提取边缘特征

```python
# ugbd_refiner.py 第64-65行
img_feat0 = F.relu(self.in_conv(image))  # B×8×H×W (浅层图像特征)
edge = wavelet_like_edges(image)          # B×1×H×W (边缘图)
```

**边缘提取**: 见2.2.1节

#### 步骤3: 特征融合

```python
# ugbd_refiner.py 第67-68行
x = torch.cat([img_feat0, prob, edge, uncert], dim=1)  # B×11×H×W
x = self.fuse(x)  # B×mid_ch×H×W (默认mid_ch=64)
```

**融合特征组成**:
- `img_feat0` (8通道): 浅层图像特征
- `prob` (1通道): 当前预测概率
- `edge` (1通道): 边缘图
- `uncert` (1通道): 不确定性图

**融合网络**:
```python
# ugbd_refiner.py 第51行
self.fuse = nn.Conv2d(8+3, mid_ch, 3, padding=1)
# 3×3卷积，将11通道融合为mid_ch通道
```

#### 步骤4: ConvGRU迭代细化

**ConvGRU Cell结构**:
```python
# ugbd_refiner.py 第24-39行
class ConvGRUCell(nn.Module):
    def __init__(self, in_ch, hid_ch):
        self.conv_zr = nn.Conv2d(in_ch + hid_ch, 2*hid_ch, 3, padding=1)
        self.conv_h  = nn.Conv2d(in_ch + hid_ch, hid_ch, 3, padding=1)
    
    def forward(self, x, h):
        if h is None:
            h = zeros(B, hid_ch, H, W)
        
        # 重置门和更新门
        zr = sigmoid(self.conv_zr(cat([x, h])))  # B×2×hid_ch×H×W
        z, r = chunk(zr, 2, dim=1)  # z:更新门, r:重置门
        
        # 候选隐藏状态
        h_tilde = tanh(self.conv_h(cat([x, h*r])))
        
        # 更新隐藏状态
        h = (1 - z) * h + z * h_tilde
        return h
```

**迭代过程**:
```python
# ugbd_refiner.py 第69-71行
h = None
for step in range(self.steps):  # 默认steps=3
    h = self.gru(x, h)
```

**伪代码**:
```
初始化: h⁰ = 0

For t = 1 to steps:
    # 重置门和更新门
    z^t, r^t = sigmoid(Conv([x, h^{t-1}]))
    
    # 候选隐藏状态
    h̃^t = tanh(Conv([x, r^t ⊙ h^{t-1}]))
    
    # 更新隐藏状态
    h^t = (1 - z^t) ⊙ h^{t-1} + z^t ⊙ h̃^t

最终: h = h^{steps}
```

**迭代公式**:
```
h^t = (1 - z^t) ⊙ h^{t-1} + z^t ⊙ tanh(Conv([x, r^t ⊙ h^{t-1}]))

其中:
  z^t = σ(Conv_z([x, h^{t-1}]))  (更新门)
  r^t = σ(Conv_r([x, h^{t-1}]))  (重置门)
  x: 融合特征 (固定输入)
  h^{t-1}: 上一步隐藏状态
```

#### 步骤5: 生成残差并输出

```python
# ugbd_refiner.py 第72-73行
res = self.out(h)  # B×1×H×W
return logits + res
```

**输出网络**:
```python
# ugbd_refiner.py 第53-57行
self.out = nn.Sequential(
    nn.Conv2d(mid_ch, mid_ch, 3, padding=1),
    nn.ReLU(inplace=True),
    nn.Conv2d(mid_ch, 1, 1)  # 输出残差
)
```

**最终公式**:
```
refined_logits = logits + Δ(logits, edge, uncertainty)

其中:
  Δ = Out(ConvGRU^steps(Fuse([img_feat, prob, edge, uncert])))
```

### 3.3 完整公式总结

**UGBD细化公式**:
```
给定:
  image: B×3×H×W
  logits: B×1×H×W

计算:
  prob = sigmoid(logits)
  uncert = 4 × prob × (1 - prob)
  img_feat = ReLU(Conv(image))
  edge = wavelet_like_edges(image)
  
  x = Fuse([img_feat, prob, edge, uncert])
  
  h⁰ = 0
  For t = 1 to steps:
      z^t, r^t = σ(Conv_zr([x, h^{t-1}]))
      h̃^t = tanh(Conv_h([x, r^t ⊙ h^{t-1}]))
      h^t = (1 - z^t) ⊙ h^{t-1} + z^t ⊙ h̃^t
  
  Δ = Out(h^{steps})
  refined_logits = logits + Δ
```

### 3.4 迭代步骤数

**默认配置**:
- **训练脚本**: `--ugbd_steps 3` (默认) 或 `--ugbd_steps 4` (激进优化)
- **代码**: `UGBDRefiner(steps=3)` 或 `UGBDRefiner(steps=4)`

**选择原则**:
- **steps=3**: 平衡效果和速度
- **steps=4**: 更精细的细化，但计算量增加
- **steps>4**: 通常收益递减

### 3.5 关键设计点

1. **不确定性引导**: 
   - 自动识别需要细化的区域（高不确定性）
   - 避免在确信区域过度优化

2. **边缘信息融合**:
   - 显式使用边缘图引导细化
   - 帮助模型关注边界区域

3. **迭代细化**:
   - ConvGRU允许信息在迭代间传递
   - 逐步改善预测质量

4. **残差连接**:
   - `logits + res` 保持原始预测的稳定性
   - 只对需要的地方进行修正

### 3.6 数据流图

```
输入: image [B×3×H×W], logits [B×1×H×W]
    ↓
┌─────────────────────────────────────┐
│ 1. 计算不确定性                      │
│    prob = sigmoid(logits)           │
│    uncert = 4 × prob × (1-prob)     │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 2. 提取特征                          │
│    img_feat = Conv(image)           │
│    edge = wavelet_edges(image)      │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 3. 特征融合                          │
│    x = Fuse([img_feat, prob,        │
│              edge, uncert])         │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 4. ConvGRU迭代细化 (steps=3)        │
│    h⁰ = 0                           │
│    h¹ = GRU(x, h⁰)                  │
│    h² = GRU(x, h¹)                  │
│    h³ = GRU(x, h²)                  │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 5. 生成残差                          │
│    res = Out(h³)                    │
└─────────────────────────────────────┘
    ↓
输出: refined_logits = logits + res
```

---

## 总结

### DINOv3特征接口
- **提取层**: [2, 5, 8, 11] (4层多尺度特征)
- **冻结策略**: 建议显式调用`lock_backbone()`冻结
- **维度对齐**: 1×1投影 → 残差网络统一 → 上采样融合

### Wavelet使用
- **损失函数**: WDL, WED, TV-L1, BFCE (4种)
- **边缘引导**: `wavelet_like_edges()` 用于UGBD和SDF

### UGBD细化
- **迭代步骤**: 默认3步，激进优化4步
- **核心公式**: `logits ← logits + Δ(logits, edge, uncertainty)`
- **迭代机制**: ConvGRU逐步细化

---

**文档版本**: v1.0  
**最后更新**: 2025年

