# SegDINOv3 + SDF + Wavelet + UGBD 模型完整梳理文档

> **版本**: 最优训练版本（基于 `train_aggressive.sh`）  
> **目标**: IEEE顶刊论文撰写支持  
> **日期**: 2025年

---

## 📑 目录

1. [模型整体架构](#1-模型整体架构)
2. [数据流程与预处理](#2-数据流程与预处理)
3. [核心模块详细解析](#3-核心模块详细解析)
4. [损失函数体系](#4-损失函数体系)
5. [训练流程详解](#5-训练流程详解)
6. [评估指标与TTA](#6-评估指标与tta)
7. [关键参数配置表](#7-关键参数配置表)
8. [数学公式汇总](#8-数学公式汇总)
9. [流程图描述](#9-流程图描述)
10. [创新点与技术亮点](#10-创新点与技术亮点)

---

## 1. 模型整体架构

### 1.1 架构概览

```
输入图像 I ∈ R^(B×3×H×W)
    ↓
┌─────────────────────────────────────────┐
│  DINOv3 Backbone (冻结)                  │
│  - 提取4层多尺度特征                      │
│  - Layer indices: [2, 5, 8, 11]          │
│  - 输出: [F1, F2, F3, F4]                │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│  DPT Head                                │
│  - 特征投影与融合                         │
│  - 输出: logits₀ ∈ R^(B×1×H×W)          │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│  Mamba Decoder                           │
│  - Token编码 (图像+概率)                 │
│  - GroupSSM处理                          │
│  - 多查询融合机制                         │
│  - 输出: logits₁ ∈ R^(B×1×H×W)          │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│  UGBD Refiner (可选)                     │
│  - 不确定性引导                          │
│  - ConvGRU迭代细化 (T=4步)               │
│  - 输出: logits₂ ∈ R^(B×1×H×W)           │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│  SDF Head (可选)                         │
│  - 预测有符号距离场                       │
│  - 几何先验校正                          │
│  - 输出: logits_final ∈ R^(B×1×H×W)     │
└─────────────────────────────────────────┘
    ↓
最终概率图 P = σ(logits_final) ∈ [0,1]^(B×1×H×W)
```

### 1.2 模块连接关系

- **Backbone → DPT**: 多尺度特征传递
- **DPT → Mamba**: 初始logits传递
- **Mamba → UGBD**: 细化logits传递
- **UGBD → SDF**: 进一步细化logits传递
- **所有模块**: 端到端可训练（Backbone冻结）

---

## 2. 数据流程与预处理

### 2.1 数据组织结构

```
segdata/
  └── {dataset_name}/
      ├── train/
      │   ├── image/  (图像文件: .jpg, .png, .bmp等)
      │   └── mask/   (掩码文件: .png)
      └── test/
          ├── image/
          └── mask/
```

### 2.2 数据预处理流程

#### 2.2.1 训练时数据增强 (`augment_underwater`)

**输入**: 
- `img_rgb`: RGB图像 (H×W×3, uint8)
- `mask`: 灰度掩码 (H×W, uint8)

**处理步骤**:

1. **随机缩放** (scale ∈ [0.75, 1.25])
   ```python
   scale = random.uniform(0.75, 1.25)
   nh, nw = int(H*scale), int(W*scale)
   img = cv2.resize(img_rgb, (nw, nh))
   msk = cv2.resize(mask, (nw, nh), interpolation=cv2.INTER_NEAREST)
   ```

2. **随机裁剪/填充**
   - 如果缩放后尺寸 ≥ 目标尺寸: 随机裁剪
   - 如果缩放后尺寸 < 目标尺寸: 中心填充

3. **水平翻转** (概率 50%)
   ```python
   if random.random() < 0.5:
       img = img[:, ::-1, :]
       msk = msk[:, ::-1]
   ```

4. **颜色抖动** (概率 80%)
   - RGB通道独立调整，模拟水下色偏
   ```python
   g = g * random.uniform(0.9, 1.15)
   b = b * random.uniform(0.95, 1.20)
   r = r * random.uniform(0.85, 1.05)
   ```

5. **亮度/对比度调整** (概率 80%)
   ```python
   img = img * random.uniform(0.85, 1.15) + random.uniform(-0.08, 0.08)
   ```

6. **高斯模糊** (概率 30%)
   ```python
   kernel_size = random.choice([(3,3), (5,5)])
   sigma = random.uniform(0.6, 1.2)
   img = cv2.GaussianBlur(img, kernel_size, sigmaX=sigma)
   ```

7. **噪声添加** (概率 25%)
   ```python
   noise = np.random.normal(0, 8, img.shape)
   img = np.clip(img + noise, 0, 255)
   ```

#### 2.2.2 最终归一化

```python
# 图像: [0, 255] → [0, 1] → Tensor (3×H×W)
img_t = torch.from_numpy(img_rgb.astype(np.float32) / 255.0)
img_t = img_t.permute(2, 0, 1)  # (H,W,3) → (3,H,W)

# 掩码: [0, 255] → {0, 1} → Tensor (1×H×W)
msk_bin = (mask > 127).astype(np.float32)
msk_t = torch.from_numpy(msk_bin[None, ...])  # (1,H,W)
```

### 2.3 验证时处理

- **无数据增强**
- **直接resize到目标尺寸** (384×384)
- **保持原始比例** (可选，通过TTA处理多尺度)

---

## 3. 核心模块详细解析

### 3.1 DINOv3 Backbone + DPT Head

#### 3.1.1 DINOv3 Backbone

**配置**:
- **模型**: DINOv3-ViT-Small (ViT-S/16)
- **预训练权重**: `dinov3_vits16_pretrain_lvd1689m-08c60483.pth`
- **状态**: **冻结** (所有参数 `requires_grad=False`)

**特征提取**:
```python
# 提取4层中间特征
intermediate_layer_idx = {
    'small': [2, 5, 8, 11],  # 第3, 6, 9, 12层
    'base':  [2, 5, 8, 11],
}

features = backbone.get_intermediate_layers(
    x, n=intermediate_layer_idx['small']
)
# 输出: [F1, F2, F3, F4]
# F1: [B, N, 96]   (Layer 2)
# F2: [B, N, 192]  (Layer 5)
# F3: [B, N, 384]  (Layer 8)
# F4: [B, N, 768]  (Layer 11)
# 其中 N = (H/16) × (W/16) = patch_h × patch_w
```

**特征维度**:
- **Small版本**: [96, 192, 384, 768]
- **Base版本**: [256, 512, 1024, 768]

#### 3.1.2 DPT Head

**结构**:

```
4层特征 [F1, F2, F3, F4]
    ↓
1×1投影层 (统一维度)
    ↓
上采样到相同尺寸 (H/16 × W/16)
    ↓
特征拼接 (通道维度)
    ↓
输出卷积 → logits₀
```

**详细实现**:

```python
class DPTHead(nn.Module):
    def __init__(self, nclass, in_channels, features=256, 
                 out_channels=[96, 192, 384, 768]):
        # 投影层: 将每层特征投影到统一维度
        self.projects = nn.ModuleList([
            nn.Conv2d(in_channels, out_ch, 1) 
            for out_ch in out_channels
        ])
        
        # 特征融合层
        self.scratch = _make_scratch(out_channels, features, groups=1)
        self.scratch.output_conv = nn.Conv2d(features*4, nclass, 1)
    
    def forward(self, out_features, patch_h, patch_w):
        # 1. 重塑特征: [B, N, C] → [B, C, H/16, W/16]
        out = []
        for i, x in enumerate(out_features):
            x = x.permute(0, 2, 1).reshape((B, C, patch_h, patch_w))
            x = self.projects[i](x)  # 投影
            out.append(x)
        
        # 2. 特征融合
        layer_1_rn = self.scratch.layer1_rn(out[0])
        layer_2_rn = self.scratch.layer2_rn(out[1])
        layer_3_rn = self.scratch.layer3_rn(out[2])
        layer_4_rn = self.scratch.layer4_rn(out[3])
        
        # 3. 上采样到相同尺寸
        target_hw = layer_1_rn.shape[-2:]
        layer_2_up = F.interpolate(layer_2_rn, size=target_hw, mode="bilinear")
        layer_3_up = F.interpolate(layer_3_rn, size=target_hw, mode="bilinear")
        layer_4_up = F.interpolate(layer_4_rn, size=target_hw, mode="bilinear")
        
        # 4. 拼接融合
        fused = torch.cat([layer_1_rn, layer_2_up, layer_3_up, layer_4_up], dim=1)
        
        # 5. 输出logits
        out = self.scratch.output_conv(fused)  # [B, 1, H/16, W/16]
        
        # 6. 上采样到原始尺寸
        out = F.interpolate(out, (H, W), mode='bilinear', align_corners=True)
        return out  # [B, 1, H, W]
```

**输出**: `logits₀ ∈ R^(B×1×H×W)`

---

### 3.2 Mamba Decoder

#### 3.2.1 整体架构

```
输入: image [B×3×H×W], logits₀ [B×1×H×W]
    ↓
ImageTokenEncoder
    ├── 图像+概率拼接 → [B×4×H×W]
    ├── 2次下采样 → [B×192×H/4×W/4]
    ├── 位置编码
    └── 展平 → tokens [B×L×192], L=(H/4)×(W/4)
    ↓
查询向量 queries [1×12×192] (可学习参数)
    ↓
GroupSSM处理 (3层)
    ├── 每层: GroupSSM1D
    ├── 查询-特征交互
    └── 残差连接
    ↓
掩码生成
    ├── 特征嵌入 → mask_feat [B×64×H/4×W/4]
    ├── 系数预测 → coeff [B×12×64]
    ├── 加权融合 → masks [B×12×H×W]
    └── IoU权重 → residual [B×1×H×W]
    ↓
输出: logits₁ = logits₀ + residual
```

#### 3.2.2 ImageTokenEncoder

**功能**: 将图像和概率图编码为token序列

```python
class ImageTokenEncoder(nn.Module):
    def __init__(self, in_ch=4, base_ch=64, out_ch=192):
        self.enc = nn.Sequential(
            # 第1次下采样: 4→32, H×W → H/2×W/2
            nn.Conv2d(4, 32, 3, stride=2, padding=1),
            nn.BatchNorm2d(32), nn.GELU(),
            
            # 第2次下采样: 32→64, H/2×W/2 → H/4×W/4
            nn.Conv2d(32, 64, 3, stride=2, padding=1),
            nn.BatchNorm2d(64), nn.GELU(),
            
            # 特征提取: 64→192, 尺寸不变
            nn.Conv2d(64, 192, 3, stride=1, padding=1),
            nn.BatchNorm2d(192), nn.GELU()
        )
        self.pos = PositionalEncoding2D(192)
    
    def forward(self, img, prob):
        # 输入: img [B×3×H×W], prob [B×1×H×W]
        x = torch.cat([img, prob], dim=1)  # [B×4×H×W]
        feat = self.enc(x)                  # [B×192×H/4×W/4]
        B, C, H4, W4 = feat.shape
        
        # 位置编码
        feat = feat + self.pos(H4, W4, feat.device)
        
        # 展平为序列
        tok = feat.flatten(2).permute(0, 2, 1)  # [B×L×192], L=H4×W4
        return feat, tok
```

**位置编码**: 2D正弦位置编码
- 分别对H和W维度编码
- 维度: 192 (可被4整除)

#### 3.2.3 GroupSSM1D

**功能**: 轻量级状态空间模块，类似Mamba的简化版本

```python
class GroupSSM1D(nn.Module):
    def __init__(self, dim=192, kernel_size=9, groups=4, expansion=2):
        # 归一化
        self.norm1 = nn.LayerNorm(dim)
        
        # 深度可分离卷积 (分组卷积)
        self.dw = nn.Conv1d(dim, dim, kernel_size, 
                           padding=kernel_size//2, 
                           groups=dim, bias=False)
        self.proj = nn.Conv1d(dim, dim, 1, bias=False)
        
        # GLU门控机制
        self.glu_u = nn.Linear(dim, dim)
        self.glu_v = nn.Linear(dim, dim)
        
        # FFN
        self.norm2 = nn.LayerNorm(dim)
        hidden = dim * expansion  # 192 * 2 = 384
        self.ff1 = nn.Linear(dim, hidden)
        self.ff2 = nn.Linear(hidden, dim)
        self.act = nn.GELU()
    
    def forward(self, x):  # x: [B×L×C]
        # 残差1: 卷积 + GLU
        idt = x
        x = self.norm1(x)
        x = self.proj(self.dw(x.transpose(1,2))).transpose(1,2)
        g = torch.sigmoid(self.glu_u(x))
        x = idt + g * self.glu_v(x)
        
        # 残差2: FFN
        idt2 = x
        x = self.norm2(x)
        x = self.ff2(self.act(self.ff1(x)))
        return idt2 + x
```

**关键参数**:
- `kernel_size=9`: 卷积核大小
- `groups=4`: 分组数（实际使用 `groups=dim` 实现深度可分离卷积）
- `expansion=2`: FFN扩展倍数

#### 3.2.4 查询机制与掩码生成

```python
class GMSAMDecoderLite(nn.Module):
    def __init__(self, num_queries=12, token_dim=192, num_layers=3, 
                 groups=4, kernel_size=9, mask_embed_dim=64):
        # 可学习查询向量
        self.queries = nn.Parameter(
            torch.randn(1, num_queries, token_dim) * 0.02
        )  # [1×12×192]
        
        # GroupSSM层
        self.layers = nn.ModuleList([
            GroupSSM1D(token_dim, kernel_size, groups) 
            for _ in range(num_layers)
        ])
        
        # Token摘要 (用于门控)
        self.tok_reduce = nn.Sequential(
            nn.LayerNorm(token_dim),
            nn.Linear(token_dim, token_dim),
            nn.GELU()
        )
        self.q_gate = nn.Sequential(
            nn.Linear(token_dim, token_dim),
            nn.Sigmoid()
        )
        
        # 掩码嵌入
        self.mask_embed = nn.Sequential(
            nn.Conv2d(token_dim, mask_embed_dim, 3, 1, 1, bias=False),
            nn.GroupNorm(8, mask_embed_dim),
            nn.GELU()
        )
        
        # 系数预测
        self.to_mask_coeff = nn.Linear(token_dim, mask_embed_dim)
        
        # IoU头 (用于权重)
        self.iou_head = nn.Sequential(
            nn.Linear(token_dim, token_dim),
            nn.GELU(),
            nn.Linear(token_dim, 1)
        )
    
    def forward(self, image, logits):
        prob = torch.sigmoid(logits)
        base_feat, tokens = self.img_tok(image, prob)
        # base_feat: [B×192×H/4×W/4]
        # tokens: [B×L×192], L=(H/4)×(W/4)
        
        B, L, C = tokens.shape
        
        # 查询向量扩展
        q = self.queries.expand(B, -1, -1)  # [B×12×192]
        
        # Token摘要 (用于门控)
        tok_mean = tokens.mean(dim=1)  # [B×192]
        tok_max = tokens.max(dim=1)[0]  # [B×192]
        summary = self.tok_reduce(tok_mean + tok_max)  # [B×192]
        
        # GroupSSM处理
        for blk in self.layers:
            # 门控机制
            gate = self.q_gate(summary).unsqueeze(1)  # [B×1×192]
            q = q + gate * q
            q = blk(q)  # [B×12×192]
        
        # 掩码生成
        mask_feat = self.mask_embed(base_feat)  # [B×64×H/4×W/4]
        coeff = self.to_mask_coeff(q)           # [B×12×64]
        
        # 外积: coeff × mask_feat
        masks_low = torch.einsum("bne,behw->bnhw", coeff, mask_feat)
        # masks_low: [B×12×H/4×W/4]
        
        # 上采样到原始尺寸
        masks = F.interpolate(masks_low, size=logits.shape[-2:], 
                            mode="bilinear", align_corners=False)
        # masks: [B×12×H×W]
        
        # IoU权重
        w = torch.sigmoid(self.iou_head(q)).squeeze(-1)  # [B×12]
        w = (w / (w.sum(dim=1, keepdim=True) + 1e-6)).unsqueeze(1)  # [B×1×12]
        
        # 加权融合
        residual = torch.bmm(w, masks.view(B, 12, -1)).view(B, 1, H, W)
        # residual: [B×1×H×W]
        
        return logits + residual
```

**关键设计**:
- **多查询机制**: 12个可学习查询向量，每个生成一个掩码
- **IoU权重**: 根据查询质量动态加权
- **残差连接**: 在原始logits上添加细化

---

### 3.3 UGBD Refiner (Uncertainty-Guided Boundary Diffusion)

#### 3.3.1 整体架构

```
输入: image [B×3×H×W], logits₁ [B×1×H×W]
    ↓
不确定性计算
    ├── prob = σ(logits₁)
    └── uncert = 4 × prob × (1 - prob)  [B×1×H×W]
    ↓
边缘提取 (小波风格)
    └── edge = wavelet_like_edges(image)  [B×1×H×W]
    ↓
特征融合
    ├── img_feat = Conv(image)  [B×8×H×W]
    ├── 拼接: [img_feat, prob, edge, uncert]  [B×11×H×W]
    └── fuse → [B×64×H×W]
    ↓
ConvGRU迭代细化 (T=4步)
    ├── h⁰ = 0
    ├── For t=1 to 4:
    │   ├── z^t, r^t = σ(Conv_zr([x, h^{t-1}]))
    │   ├── h̃^t = tanh(Conv_h([x, r^t ⊙ h^{t-1}]))
    │   └── h^t = (1-z^t) ⊙ h^{t-1} + z^t ⊙ h̃^t
    └── h^4
    ↓
输出卷积
    └── residual = Out(h^4)  [B×1×H×W]
    ↓
输出: logits₂ = logits₁ + residual
```

#### 3.3.2 不确定性计算

```python
prob = torch.sigmoid(logits)
uncert = 4.0 * prob * (1.0 - prob)  # [0, 1]
```

**物理意义**:
- `prob=0.5` 时，`uncert=1.0` (最大不确定性)
- `prob=0` 或 `prob=1` 时，`uncert=0` (确定)

#### 3.3.3 小波风格边缘提取

```python
def wavelet_like_edges(x: torch.Tensor) -> torch.Tensor:
    # x: [B×3×H×W]
    # 3个固定高通核
    kx = [[-1,-1,-1], [ 2, 2, 2], [-1,-1,-1]]  # 水平
    ky = [[-1, 2,-1], [-1, 2,-1], [-1, 2,-1]]  # 垂直
    kd = [[ 2,-1,-1], [-1, 2,-1], [-1,-1, 2]]  # 对角
    
    # 分组卷积
    ex = F.conv2d(x, kx, groups=x.size(1), padding=1)
    ey = F.conv2d(x, ky, groups=x.size(1), padding=1)
    ed = F.conv2d(x, kd, groups=x.size(1), padding=1)
    
    # 边缘幅度
    e = torch.sqrt(ex.pow(2) + ey.pow(2) + ed.pow(2) + 1e-6)
    e = e.mean(1, keepdim=True)  # [B×1×H×W]
    
    # 归一化
    e = e / (e.amax(dim=(2,3), keepdim=True) + 1e-6)
    return e
```

#### 3.3.4 ConvGRU Cell

```python
class ConvGRUCell(nn.Module):
    def __init__(self, in_ch, hid_ch):
        # 重置门和更新门
        self.conv_zr = nn.Conv2d(in_ch + hid_ch, 2*hid_ch, 3, padding=1)
        # 候选隐藏状态
        self.conv_h = nn.Conv2d(in_ch + hid_ch, hid_ch, 3, padding=1)
    
    def forward(self, x, h):
        if h is None:
            h = torch.zeros_like(x[:, :self.conv_h.out_channels])
        
        # 拼接输入和隐藏状态
        cat = torch.cat([x, h], dim=1)
        
        # 重置门和更新门
        zr = torch.sigmoid(self.conv_zr(cat))
        z, r = torch.chunk(zr, 2, dim=1)
        
        # 候选隐藏状态
        cat2 = torch.cat([x, h * r], dim=1)
        h_tilde = torch.tanh(self.conv_h(cat2))
        
        # 更新隐藏状态
        h = (1 - z) * h + z * h_tilde
        return h
```

**迭代公式**:
```
h⁰ = 0
For t = 1 to T (T=4):
    z^t, r^t = σ(Conv_zr([x, h^{t-1}]))
    h̃^t = tanh(Conv_h([x, r^t ⊙ h^{t-1}]))
    h^t = (1 - z^t) ⊙ h^{t-1} + z^t ⊙ h̃^t
```

#### 3.3.5 UGBD完整实现

```python
class UGBDRefiner(nn.Module):
    def __init__(self, mid_ch=64, steps=4):
        self.steps = steps
        self.in_conv = nn.Conv2d(3, 8, 3, padding=1)
        self.fuse = nn.Conv2d(8+3, mid_ch, 3, padding=1)  # 8+1+1+1=11
        self.gru = ConvGRUCell(mid_ch, mid_ch)
        self.out = nn.Sequential(
            nn.Conv2d(mid_ch, mid_ch, 3, padding=1),
            nn.ReLU(inplace=True),
            nn.Conv2d(mid_ch, 1, 1)
        )
    
    def forward(self, image, logits):
        # 不确定性
        prob = torch.sigmoid(logits)
        uncert = 4.0 * prob * (1.0 - prob)
        
        # 边缘
        img_feat0 = F.relu(self.in_conv(image))
        edge = wavelet_like_edges(image)
        
        # 特征融合
        x = torch.cat([img_feat0, prob, edge, uncert], dim=1)  # [B×11×H×W]
        x = self.fuse(x)  # [B×64×H×W]
        
        # ConvGRU迭代
        h = None
        for _ in range(self.steps):
            h = self.gru(x, h)
        
        # 输出残差
        res = self.out(h)  # [B×1×H×W]
        return logits + res
```

**关键参数**:
- `steps=4`: 迭代步数（最优配置）
- `mid_ch=64`: 中间通道数

---

### 3.4 SDF Head (Signed Distance Field)

#### 3.4.1 整体架构

```
输入: image [B×3×H×W], logits₂ [B×1×H×W]
    ↓
边缘提取
    └── edge = wavelet_like_edges(image)  [B×1×H×W]
    ↓
特征拼接
    └── x = [image, prob, edge]  [B×5×H×W]
    ↓
编码器 (3层卷积)
    ├── Conv(5→64) + GroupNorm + GELU
    ├── Conv(64→64) + GroupNorm + GELU
    └── Conv(64→64) + GroupNorm + GELU
    ↓
SDF预测
    └── sdf = Conv(64→1)  [B×1×H×W]
    ↓
残差融合
    └── logits_final = logits₂ - λ × sdf
```

#### 3.4.2 SDF Head实现

```python
class SDFHead(nn.Module):
    def __init__(self, mid_ch=64, fuse_lambda=0.35):
        self.fuse_lambda = fuse_lambda
        in_ch = 3 + 1 + 1  # image + prob + edge
        
        self.enc = nn.Sequential(
            nn.Conv2d(in_ch, mid_ch, 3, padding=1, bias=False),
            nn.GroupNorm(8, mid_ch),
            nn.GELU(),
            nn.Conv2d(mid_ch, mid_ch, 3, padding=1, bias=False),
            nn.GroupNorm(8, mid_ch),
            nn.GELU(),
            nn.Conv2d(mid_ch, mid_ch, 3, padding=1, bias=False),
            nn.GroupNorm(8, mid_ch),
            nn.GELU(),
        )
        self.to_sdf = nn.Conv2d(mid_ch, 1, 1)
    
    def forward(self, image, logits):
        prob = torch.sigmoid(logits)
        edge = wavelet_like_edges(image)
        
        # 特征拼接
        x = torch.cat([image, prob, edge], dim=1)  # [B×5×H×W]
        
        # 编码
        feat = self.enc(x)  # [B×64×H×W]
        
        # SDF预测
        sdf = self.to_sdf(feat)  # [B×1×H×W]
        
        # 残差融合
        refined = logits - self.fuse_lambda * sdf
        return refined, sdf
```

**融合公式**:
```
logits_final = logits₂ - λ × SDF_pred

其中:
  λ = fuse_lambda = 0.35 (最优配置)
  SDF_pred: 预测的有符号距离场
```

**物理意义**:
- SDF值越大（距离边界越远）→ logits减小越多
- SDF值越小（接近边界）→ logits减小越少
- 通过几何先验校正边界

#### 3.4.3 SDF Ground Truth生成

```python
def gt_sdf_from_mask(mask_np):
    """
    mask_np: H×W (0/1)
    标准有符号距离:
      - 前景内部: 负值 (距离边界的距离)
      - 前景外部: 正值 (距离边界的距离)
    """
    m = (mask_np > 0).astype("uint8")
    
    if m.sum() == 0:
        # 全背景
        dist_out = distance_transform_edt(1 - m)
        sdf = dist_out
    elif m.sum() == m.size:
        # 全前景
        dist_in = distance_transform_edt(m)
        sdf = -dist_in
    else:
        # 正常情况
        dist_out = distance_transform_edt(1 - m)  # 外部距离
        dist_in = distance_transform_edt(m)        # 内部距离
        sdf = dist_out - dist_in
    
    return sdf
```

**归一化与缩放**:
```python
def batch_build_sdf(gt_masks, scale=0.7):
    # 归一化到 roughly [-1, 1]
    s = max(1.0, float(abs(sdf).max()))
    sdf = (sdf / s) * scale
    return sdf
```

---

## 4. 损失函数体系

### 4.1 损失函数总览

总损失公式:
```
L_total = w_bce × L_BCE
        + w_iou × L_IoU
        + w_ssim × L_SSIM
        + w_wav × L_wavelet_detail
        + w_wed × L_wavelet_edge_dice
        + w_tv × L_TV
        + w_bfce × L_boundary_focal
        + w_biou × L_boundary_iou
        + w_uwb × L_uncertainty_weighted_boundary
        + w_freq × L_fourier
        + w_cldice × L_clDice
        + w_sdf × L_SDF_L1
        + w_eik × L_Eikonal
        + w_curv × L_Curvature
```

### 4.2 基础损失函数

#### 4.2.1 BCE Loss

```python
L_BCE = BCEWithLogitsLoss(logits, target)
```

**权重**: `w_bce = 1.0`

#### 4.2.2 IoU Loss

```python
def iou_loss(prob, target, eps=1e-6):
    inter = (prob * target).sum(dim=(1,2,3))
    union = prob.sum(dim=(1,2,3)) + target.sum(dim=(1,2,3)) - inter + eps
    iou = (inter + eps) / union
    return (1 - iou).mean()
```

**权重**: `w_iou = 0.7`

#### 4.2.3 SSIM Loss

```python
def ssim_loss(prob, target):
    # 局部SSIM (7×7窗口)
    mu_x = F.avg_pool2d(prob, 7, 1, 3)
    mu_y = F.avg_pool2d(target, 7, 1, 3)
    sigma_x = F.avg_pool2d(prob*prob, 7, 1, 3) - mu_x*mu_x
    sigma_y = F.avg_pool2d(target*target, 7, 1, 3) - mu_y*mu_y
    sigma_xy = F.avg_pool2d(prob*target, 7, 1, 3) - mu_x*mu_y
    
    c1, c2 = 0.01**2, 0.03**2
    num = (2*mu_x*mu_y + c1) * (2*sigma_xy + c2)
    den = (mu_x*mu_x + mu_y*mu_y + c1) * (sigma_x + sigma_y + c2)
    ssim = num / (den + 1e-12)
    return (1 - ssim).mean()
```

**权重**: `w_ssim = 0.45`

### 4.3 小波域损失函数

#### 4.3.1 小波分解 (Haar)

```python
def dwt2_haar(x):
    # 2×2 Haar小波核
    h0 = [1, 1] / √2
    h1 = [1, -1] / √2
    
    LL = h0 ⊗ h0  # 低频
    LH = h0 ⊗ h1  # 水平高频
    HL = h1 ⊗ h0  # 垂直高频
    HH = h1 ⊗ h1  # 对角高频
    
    # 分组卷积 (stride=2)
    y = F.conv2d(x, [LL, LH, HL, HH], stride=2, groups=C)
    # 输出: [B, 4C, H/2, W/2]
    
    return LL, (LH, HL, HH)
```

#### 4.3.2 Wavelet Detail Loss

```python
def wavelet_detail_loss(prob, gt, levels=3):
    plist = dwt2_haar_levels(prob, levels=levels)
    glist = dwt2_haar_levels(gt, levels=levels)
    
    losses = []
    for (PLL, (PLH, PHL, PHH)), (GLL, (GLH, GHL, GHH)) in zip(plist, glist):
        # 高频幅度
        Pmag = |PLH| + |PHL| + |PHH|
        Gmag = |GLH| + |GHL| + |GHH|
        
        # L1损失
        losses.append(F.l1_loss(Pmag, Gmag))
    
    return sum(losses) / len(losses)
```

**权重**: `w_wav = 0.35`

#### 4.3.3 Wavelet Edge Dice

```python
def wavelet_edge_dice(prob, gt, levels=3):
    plist = dwt2_haar_levels(prob, levels=levels)
    glist = dwt2_haar_levels(gt, levels=levels)
    
    losses = []
    for (PLL, (PLH, PHL, PHH)), (GLL, (GLH, GHL, GHH)) in zip(plist, glist):
        Pmag = |PLH| + |PHL| + |PHH|
        Gmag = |GLH| + |GHL| + |GHH|
        
        # 归一化
        Pn = (Pmag - Pmag.min()) / (Pmag.max() - Pmag.min() + eps)
        Gn = (Gmag - Gmag.min()) / (Gmag.max() - Gmag.min() + eps)
        
        # Dice损失
        inter = (Pn * Gn).sum(dim=(1,2,3))
        den = Pn.sum(dim=(1,2,3)) + Gn.sum(dim=(1,2,3)) + eps
        dice = (2*inter + eps) / den
        losses.append(1 - dice.mean())
    
    return sum(losses) / len(losses)
```

**权重**: `w_wed = 0.15`

#### 4.3.4 TV-L1 Loss

```python
def tv_l1_loss(prob):
    dx = |prob[:,:,:,1:] - prob[:,:,:,:-1]|.mean()
    dy = |prob[:,:,1:,:] - prob[:,:,:-1,:]|.mean()
    return dx + dy
```

**权重**: `w_tv = 0.10`

#### 4.3.5 Boundary Focal BCE

```python
def boundary_focal_bce_loss(logits, gt, lam=1.5, gamma=2.0):
    p = torch.sigmoid(logits).clamp(1e-6, 1-1e-6)
    bce = F.binary_cross_entropy_with_logits(logits, gt, reduction="none")
    
    # 边界带
    band = _morph_band(gt)  # 形态学边界带
    
    # 不确定性
    u = 4.0 * p * (1.0 - p)
    
    # 权重
    w = 1.0 + lam * band * (u ** gamma)
    
    return (w * bce).mean()
```

**权重**: `w_bfce = 0.18`, `bf_lam = 1.5`

### 4.4 边界优化损失

#### 4.4.1 Boundary IoU Loss

```python
def boundary_iou_loss_from_probs(prob, target):
    # Sobel边缘
    pe = _sobel_edge(prob)   # [B×1×H×W]
    ge = _sobel_edge(target)   # [B×1×H×W]
    
    # IoU计算
    inter = (pe * ge).sum(dim=(1,2,3))
    union = pe.sum(dim=(1,2,3)) + ge.sum(dim=(1,2,3)) - inter + eps
    biou = (inter + eps) / union
    
    return (1 - biou).mean()
```

**权重**: `w_biou = 0.25`

#### 4.4.2 Uncertainty-Weighted Boundary Loss

```python
def uncertainty_weighted_boundary_loss(prob, gt, lam=2.5):
    # 边界带
    band = _morph_band(gt)
    
    # 不确定性 (熵)
    p = prob.clamp(1e-6, 1-1e-6)
    unc = -(p * log(p) + (1-p) * log(1-p))
    unc = (unc - unc.min()) / (unc.max() - unc.min() + eps)
    
    # 权重
    w = 1.0 + lam * band * unc
    
    # 加权L1 + Boundary IoU
    l1 = (w * |prob - gt|).mean()
    biou = boundary_iou_loss_from_probs(prob, gt)
    
    return l1 + biou
```

**权重**: `w_uwb = 0.20`, `uwb_lam = 2.5`

#### 4.4.3 Fourier Magnitude Loss

```python
def fourier_mag_loss(prob, target, log_mag=True, highfreq_boost=0.25):
    # FFT
    Pf = torch.fft.rfft2(prob, dim=(-2,-1))
    Tf = torch.fft.rfft2(target, dim=(-2,-1))
    
    # 幅度
    Pm = |Pf|
    Tm = |Tf|
    
    if log_mag:
        Pm = log(1 + Pm)
        Tm = log(1 + Tm)
    
    # 高频增强权重
    H, W2 = Pm.shape[-2:]
    yy = linspace(-1, 1, H)
    xx = linspace(0, 1, W2)
    r = sqrt(xx² + yy²)  # 归一化频率
    w = 1 + highfreq_boost * r
    
    return (w * |Pm - Tm|).mean()
```

**权重**: `w_freq = 0.10`, `freq_hf_boost = 0.25`

### 4.5 拓扑损失

#### 4.5.1 clDice Loss

```python
def cldice_loss(logits, target, skel_iters=10):
    prob = torch.sigmoid(logits)
    
    # 软骨架化
    SkelP = soft_skeletonize(prob, iters=skel_iters)
    SkelG = soft_skeletonize(target, iters=skel_iters)
    
    # 拓扑精度和召回
    tprec = (SkelP * target).sum() / (SkelP.sum() + eps)
    tsens = (SkelG * prob).sum() / (SkelG.sum() + eps)
    
    # clDice
    cl = (2 * tprec * tsens) / (tprec + tsens + eps)
    return 1 - cl.mean()
```

**权重**: `w_cldice = 0.08`

### 4.6 SDF形状先验损失

#### 4.6.1 SDF L1 Loss

```python
L_SDF_L1 = L1(SDF_pred, SDF_gt)
```

**权重**: `w_sdf = 0.15`

#### 4.6.2 Eikonal Loss

```python
def eikonal_loss(sdf, target_norm=1.0):
    # Sobel梯度
    gx, gy = sobel_grad(sdf)
    g = sqrt(gx² + gy² + eps)
    
    # Eikonal约束: |∇SDF| = 1
    return ((g - target_norm)²).mean()
```

**权重**: `w_eik = 0.08`

#### 4.6.3 Curvature Loss

```python
def curvature_loss(sdf):
    # 拉普拉斯算子 (曲率)
    curv = laplace(sdf)
    return (curv²).mean()
```

**权重**: `w_curv = 0.04`

### 4.7 损失权重配置表

| 损失函数 | 权重 | 说明 |
|---------|------|------|
| **BCE** | 1.0 | 基础分类损失 |
| **IoU** | 0.7 | 重叠度损失 |
| **SSIM** | 0.45 | 结构相似性 |
| **Wavelet Detail** | 0.35 | 小波细节损失 |
| **Wavelet Edge Dice** | 0.15 | 小波边缘Dice |
| **TV-L1** | 0.10 | 总变分正则化 |
| **Boundary Focal BCE** | 0.18 | 边界聚焦损失 (λ=1.5) |
| **Boundary IoU** | 0.25 | 边界IoU损失 |
| **UWB** | 0.20 | 不确定性加权边界 (λ=2.5) |
| **Fourier** | 0.10 | 傅里叶幅度损失 (高频增强=0.25) |
| **clDice** | 0.08 | 中心线Dice |
| **SDF L1** | 0.15 | SDF预测损失 |
| **Eikonal** | 0.08 | 梯度模长约束 |
| **Curvature** | 0.04 | 曲率平滑约束 |

---

## 5. 训练流程详解

### 5.1 训练配置 (最优版本)

**基础配置**:
- **数据集**: USOD10K
- **输入尺寸**: 384×384
- **批次大小**: 8
- **训练轮数**: 120 epochs
- **随机种子**: 42

**优化器**:
- **类型**: AdamW
- **学习率**: 6e-5
- **Weight Decay**: 1.5e-4

**学习率调度**:
- **Warmup**: 12 epochs
- **主调度**: Cosine Annealing
- **总epochs**: 120

**正则化**:
- **梯度裁剪**: 0.8
- **EMA**: 启用 (decay=0.999)

### 5.2 训练循环

```python
for epoch in range(1, epochs+1):
    # ========== 训练阶段 ==========
    backbone.train()
    decoder.train()
    if refine is not None: refine.train()
    if sdf_head is not None: sdf_head.train()
    
    for imgs, gts, _ in train_loader:
        # 前向传播
        imgs, gts = imgs.to(device), gts.to(device)
        
        # 1. Backbone + DPT
        logits = decoder(imgs, backbone(imgs))
        
        # 2. Mamba Decoder (已在decoder中)
        # logits已更新
        
        # 3. UGBD Refiner
        if refine is not None:
            logits = refine(imgs, logits)
        
        # 4. SDF Head
        sdf_map = None
        if sdf_head is not None:
            logits, sdf_map = sdf_head(imgs, logits)
        
        # 损失计算
        prob = torch.sigmoid(logits)
        loss = 0.0
        
        # 基础损失
        loss += w_bce * BCE(logits, gts)
        loss += w_iou * IoU(prob, gts)
        loss += w_ssim * SSIM(prob, gts)
        
        # 小波损失
        if use_wavelet:
            loss += w_wav * wavelet_detail_loss(prob, gts)
            loss += w_wed * wavelet_edge_dice(prob, gts)
            loss += w_tv * tv_l1_loss(prob)
            loss += w_bfce * boundary_focal_bce_loss(logits, gts)
        
        # 边界损失
        loss += w_biou * boundary_iou_loss_from_probs(prob, gts)
        loss += w_uwb * uncertainty_weighted_boundary_loss(prob, gts)
        loss += w_freq * fourier_mag_loss(prob, gts)
        
        # 拓扑损失
        loss += w_cldice * cldice_loss(logits, gts)
        
        # SDF损失
        if sdf_map is not None:
            with torch.no_grad():
                sdf_gt = batch_build_sdf(gts, scale=sdf_scale)
            loss += w_sdf * L1(sdf_map, sdf_gt)
            loss += w_eik * eikonal_loss(sdf_map)
            loss += w_curv * curvature_loss(sdf_map)
        
        # 反向传播
        optimizer.zero_grad()
        loss.backward()
        
        # 梯度裁剪
        if grad_clip > 0:
            torch.nn.utils.clip_grad_norm_(
                all_params, max_norm=grad_clip
            )
        
        optimizer.step()
        
        # EMA更新
        if ema is not None:
            ema.update(model)
    
    # ========== 验证阶段 ==========
    backbone.eval()
    decoder.eval()
    if refine is not None: refine.eval()
    if sdf_head is not None: sdf_head.eval()
    
    metrics = evaluate_with_module(
        backbone, decoder, val_loader, device, args,
        refine=refine, sdf_head=sdf_head
    )
    
    # 保存检查点
    save_checkpoint(epoch, metrics)
    
    # 学习率更新
    scheduler.step()
```

### 5.3 EMA (Exponential Moving Average)

```python
class EMA:
    def __init__(self, model, decay=0.999):
        self.decay = decay
        self.shadow = {}
        for n, p in model.named_parameters():
            if p.requires_grad:
                self.shadow[n] = p.detach().clone()
    
    @torch.no_grad()
    def update(self, model):
        for n, p in model.named_parameters():
            if p.requires_grad:
                self.shadow[n].mul_(self.decay).add_(
                    p.detach(), alpha=1.0 - self.decay
                )
```

**作用**: 平滑模型参数，提升泛化性能

---

## 6. 评估指标与TTA

### 6.1 评估指标

#### 6.1.1 MAE (Mean Absolute Error)

```python
MAE = |prob - gt|.mean()
```

**范围**: [0, 1]，越小越好

#### 6.1.2 mIoU (mean Intersection over Union)

```python
pred = (prob > 0.5).float()
inter = (pred * gt).sum()
union = pred.sum() + gt.sum() - inter
mIoU = (inter + eps) / (union + eps)
```

**范围**: [0, 1]，越大越好

#### 6.1.3 S-measure (Sα)

**公式**:
```
Sα = α × So + (1-α) × Sr

其中:
  So: 对象感知相似性
  Sr: 区域相似性
  α = 0.5
```

**范围**: [0, 1]，越大越好

#### 6.1.4 Fwβ (Weighted F-measure)

**公式**:
```
Fwβ = (1 + β²) × (R × P) / (R + β² × P)

其中:
  R: 加权召回率
  P: 加权精确率
  β = 0.3
```

**范围**: [0, 1]，越大越好

#### 6.1.5 E-measure (mEφ)

**公式**:
```
E = ((align + 1)² / 4).mean()

其中:
  align = 2×(pred-μ_pred)×(gt-μ_gt) / ((pred-μ_pred)² + (gt-μ_gt)²)
```

**范围**: [0, 1]，越大越好

#### 6.1.6 maxF

**公式**:
```
maxF = max_{t∈[0,1]} Fβ((prob > t).float(), gt)
```

**范围**: [0, 1]，越大越好

### 6.2 TTA (Test Time Augmentation)

#### 6.2.1 TTA策略

**多尺度**:
- 尺度: [1.0, 0.75, 1.25]

**翻转**:
- 水平翻转: 是/否

**融合方式**:
- 平均融合

#### 6.2.2 TTA实现

```python
@torch.no_grad()
def forward_with_tta(backbone, decoder, x, scales=(1.0,0.75,1.25), do_flip=True):
    outs = []
    B, C, H, W = x.shape
    
    for s in scales:
        # 缩放
        if s == 1.0:
            xi = x
        else:
            xi = F.interpolate(x, scale_factor=s, mode="bilinear", align_corners=False)
        
        # 翻转
        for flip in ([False, True] if do_flip else [False]):
            xif = torch.flip(xi, dims=[3]) if flip else xi
            
            # 前向传播
            logits = decoder(xif, backbone(xif))
            
            # 恢复到原始尺寸
            if logits.shape[-2:] != (H, W):
                logits = F.interpolate(logits, size=(H, W), mode="bilinear", align_corners=False)
            
            # 翻转回来
            if flip:
                logits = torch.flip(logits, dims=[3])
            
            # 转换为概率
            outs.append(torch.sigmoid(logits))
    
    # 平均融合
    prob = torch.mean(torch.stack(outs, dim=0), dim=0)
    
    # 转换回logits
    return torch.logit(prob.clamp(1e-6, 1-1e-6))
```

**总预测数**: 3尺度 × 2翻转 = 6次预测

---

## 7. 关键参数配置表

### 7.1 模型架构参数

| 参数 | 值 | 说明 |
|-----|-----|------|
| **DINOv3 Size** | Small | ViT-S/16 |
| **Block Indices** | [2, 5, 8, 11] | 特征提取层 |
| **输入尺寸** | 384×384 | H×W |
| **Mamba Queries** | 12 | 查询向量数量 |
| **Mamba Dim** | 192 | Token维度 |
| **Mamba Layers** | 3 | GroupSSM层数 |
| **Mamba Groups** | 4 | 分组数 |
| **Mamba Kernel** | 9 | 卷积核大小 |
| **UGBD Steps** | 4 | ConvGRU迭代步数 |
| **UGBD Mid Ch** | 64 | 中间通道数 |
| **SDF Mid Ch** | 64 | SDF编码器通道数 |
| **SDF Lambda** | 0.35 | SDF融合权重 |

### 7.2 训练超参数

| 参数 | 值 | 说明 |
|-----|-----|------|
| **Batch Size** | 8 | 批次大小 |
| **Epochs** | 120 | 训练轮数 |
| **Learning Rate** | 6e-5 | 初始学习率 |
| **Weight Decay** | 1.5e-4 | 权重衰减 |
| **Warmup Epochs** | 12 | 预热轮数 |
| **Scheduler** | Cosine | 学习率调度 |
| **Gradient Clip** | 0.8 | 梯度裁剪阈值 |
| **EMA Decay** | 0.999 | EMA衰减率 |

### 7.3 损失权重参数

见 [4.7 损失权重配置表](#47-损失权重配置表)

---

## 8. 数学公式汇总

### 8.1 前向传播公式

#### 8.1.1 DPT Head

```
F_i = Backbone.get_intermediate_layers(x, n=[2,5,8,11])
F_i' = Project_i(F_i)  # 1×1卷积投影
F_i'' = Upsample(F_i')  # 上采样到相同尺寸
F_fused = Concat([F_1'', F_2'', F_3'', F_4''])
logits₀ = Conv(F_fused)
```

#### 8.1.2 Mamba Decoder

```
prob₀ = σ(logits₀)
tokens = ImageTokenEncoder(image, prob₀)
q = Queries.expand(B, -1, -1)
summary = Reduce(tokens.mean + tokens.max)
For i in range(num_layers):
    gate = Gate(summary)
    q = q + gate * q
    q = GroupSSM_i(q)
coeff = Linear(q)
mask_feat = Embed(base_feat)
masks = Einsum(coeff, mask_feat)
w = Softmax(IoUHead(q))
residual = WeightedSum(w, masks)
logits₁ = logits₀ + residual
```

#### 8.1.3 UGBD Refiner

```
prob₁ = σ(logits₁)
uncert = 4 × prob₁ × (1 - prob₁)
edge = WaveletEdges(image)
x = Concat([Conv(image), prob₁, edge, uncert])
h⁰ = 0
For t = 1 to T:
    z^t, r^t = σ(Conv_zr([x, h^{t-1}]))
    h̃^t = tanh(Conv_h([x, r^t ⊙ h^{t-1}]))
    h^t = (1 - z^t) ⊙ h^{t-1} + z^t ⊙ h̃^t
residual = Out(h^T)
logits₂ = logits₁ + residual
```

#### 8.1.4 SDF Head

```
prob₂ = σ(logits₂)
edge = WaveletEdges(image)
x = Concat([image, prob₂, edge])
feat = Encoder(x)
SDF_pred = Conv(feat)
logits_final = logits₂ - λ × SDF_pred
```

### 8.2 损失函数公式

#### 8.2.1 基础损失

```
L_BCE = BCEWithLogitsLoss(logits, target)
L_IoU = 1 - (inter + eps) / (union + eps)
L_SSIM = 1 - SSIM(prob, target)
```

#### 8.2.2 小波域损失

```
# 小波分解
LL, (LH, HL, HH) = DWT_Haar(x)

# Wavelet Detail Loss
L_wav = (1/L) × Σ_l L1(|PLH| + |PHL| + |PHH|, |GLH| + |GHL| + |GHH|)

# Wavelet Edge Dice
L_wed = (1/L) × Σ_l (1 - Dice(Pmag_norm, Gmag_norm))

# TV-L1
L_TV = |∂prob/∂x|.mean() + |∂prob/∂y|.mean()
```

#### 8.2.3 边界损失

```
# Boundary IoU
L_biou = 1 - (inter_edge + eps) / (union_edge + eps)

# Uncertainty-Weighted Boundary
unc = -[p×log(p) + (1-p)×log(1-p)]
w = 1 + λ × band × unc
L_uwb = (w × |prob - gt|).mean() + L_biou

# Fourier Magnitude
L_freq = (w_freq × |FFT(prob)| - |FFT(gt)||).mean()
```

#### 8.2.4 SDF损失

```
# SDF L1
L_SDF = L1(SDF_pred, SDF_gt)

# Eikonal
L_eik = (|∇SDF| - 1)²

# Curvature
L_curv = (ΔSDF)²
```

---

## 9. 流程图描述

### 9.1 整体前向传播流程图

```
┌─────────────────────────────────────────────────────────────┐
│                    输入图像 I [B×3×H×W]                      │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│              DINOv3 Backbone (冻结)                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │ Layer 2  │  │ Layer 5  │  │ Layer 8  │  │ Layer 11 │      │
│  │ [B,N,96] │  │ [B,N,192]│  │ [B,N,384]│  │ [B,N,768]│      │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘      │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                      DPT Head                                │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │ Project  │  │ Project  │  │ Project  │  │ Project  │    │
│  │    ↓     │  │    ↓     │  │    ↓     │  │    ↓     │    │
│  │ Upsample │  │ Upsample │  │ Upsample │  │ Upsample │    │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘    │
│       └─────────────┴─────────────┴─────────────┘           │
│                        Concat                                │
│                           ↓                                  │
│                    Output Conv                               │
│                  logits₀ [B×1×H×W]                          │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    Mamba Decoder                             │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ ImageTokenEncoder                                    │    │
│  │  [image, prob₀] → [B×4×H×W]                         │    │
│  │  → Conv(4→32) → Conv(32→64) → Conv(64→192)        │    │
│  │  → [B×192×H/4×W/4] + PosEnc                         │    │
│  │  → tokens [B×L×192]                                 │    │
│  └──────────────────────────────────────────────────────┘    │
│                            ↓                                 │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ Query Processing                                     │    │
│  │  q = Queries [1×12×192]                              │    │
│  │  summary = Reduce(tokens)                            │    │
│  │  For i in [0, 1, 2]:                                │    │
│  │    gate = Gate(summary)                              │    │
│  │    q = q + gate × q                                  │    │
│  │    q = GroupSSM_i(q)                                 │    │
│  └──────────────────────────────────────────────────────┘    │
│                            ↓                                 │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ Mask Generation                                      │    │
│  │  mask_feat = Embed(base_feat) [B×64×H/4×W/4]         │    │
│  │  coeff = Linear(q) [B×12×64]                         │    │
│  │  masks = Einsum(coeff, mask_feat) [B×12×H×W]         │    │
│  │  w = Softmax(IoUHead(q)) [B×1×12]                    │    │
│  │  residual = WeightedSum(w, masks) [B×1×H×W]          │    │
│  └──────────────────────────────────────────────────────┘    │
│                  logits₁ = logits₀ + residual                  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                  UGBD Refiner (可选)                        │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ Feature Extraction                                   │    │
│  │  prob₁ = σ(logits₁)                                   │    │
│  │  uncert = 4 × prob₁ × (1 - prob₁)                    │    │
│  │  edge = WaveletEdges(image)                           │    │
│  │  img_feat = Conv(image)                               │    │
│  │  x = Concat([img_feat, prob₁, edge, uncert])         │    │
│  └──────────────────────────────────────────────────────┘    │
│                            ↓                                 │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ ConvGRU Iteration (T=4)                              │    │
│  │  h⁰ = 0                                              │    │
│  │  For t = 1 to 4:                                     │    │
│  │    z^t, r^t = σ(Conv_zr([x, h^{t-1}]))               │    │
│  │    h̃^t = tanh(Conv_h([x, r^t ⊙ h^{t-1}]))            │    │
│  │    h^t = (1-z^t) ⊙ h^{t-1} + z^t ⊙ h̃^t                │    │
│  └──────────────────────────────────────────────────────┘    │
│                            ↓                                 │
│                    residual = Out(h^4)                        │
│                  logits₂ = logits₁ + residual                 │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    SDF Head (可选)                          │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ SDF Prediction                                       │    │
│  │  prob₂ = σ(logits₂)                                   │    │
│  │  edge = WaveletEdges(image)                           │    │
│  │  x = Concat([image, prob₂, edge])                     │    │
│  │  feat = Encoder(x) [B×64×H×W]                        │    │
│  │  SDF_pred = Conv(feat) [B×1×H×W]                     │    │
│  └──────────────────────────────────────────────────────┘    │
│                            ↓                                 │
│              logits_final = logits₂ - λ × SDF_pred            │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│             最终输出: P = σ(logits_final) [B×1×H×W]          │
└─────────────────────────────────────────────────────────────┘
```

### 9.2 训练流程图

```
┌─────────────────────────────────────────────────────────────┐
│                    训练循环开始                               │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  For epoch = 1 to 120:                                       │
│    ┌──────────────────────────────────────────────────┐    │
│    │  训练阶段 (train_one_epoch)                        │    │
│    │  For batch in train_loader:                       │    │
│    │    ├── 前向传播                                     │    │
│    │    │   ├── Backbone + DPT → logits₀                │    │
│    │    │   ├── Mamba Decoder → logits₁                 │    │
│    │    │   ├── UGBD Refiner → logits₂                  │    │
│    │    │   └── SDF Head → logits_final                 │    │
│    │    │                                                 │    │
│    │    ├── 损失计算                                      │    │
│    │    │   ├── 基础损失 (BCE, IoU, SSIM)                │    │
│    │    │   ├── 小波损失 (Wav, WED, TV, BFCE)            │    │
│    │    │   ├── 边界损失 (BIoU, UWB, Fourier)            │    │
│    │    │   ├── 拓扑损失 (clDice)                        │    │
│    │    │   └── SDF损失 (L1, Eikonal, Curvature)         │    │
│    │    │                                                 │    │
│    │    ├── 反向传播                                      │    │
│    │    │   ├── loss.backward()                          │    │
│    │    │   ├── 梯度裁剪 (max_norm=0.8)                   │    │
│    │    │   └── optimizer.step()                         │    │
│    │    │                                                 │    │
│    │    └── EMA更新 (decay=0.999)                         │    │
│    └──────────────────────────────────────────────────┘    │
│                            ↓                                 │
│    ┌──────────────────────────────────────────────────┐    │
│    │  验证阶段 (evaluate_with_module)                  │    │
│    │  ├── 模型设置为eval()                             │    │
│    │  ├── (可选) TTA前向传播                            │    │
│    │  ├── 计算评估指标                                   │    │
│    │  │   ├── MAE, mIoU, Sα, Fwβ                        │    │
│    │  │   ├── mEφ, E_φ_adp, maxF                        │    │
│    │  │   └── 保存到CSV                                  │    │
│    │  └── 保存最佳模型                                   │    │
│    └──────────────────────────────────────────────────┘    │
│                            ↓                                 │
│    ┌──────────────────────────────────────────────────┐    │
│    │  学习率更新                                        │    │
│    │  scheduler.step()                                 │    │
│    └──────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    训练完成                                  │
│              输出最佳指标统计                                 │
└─────────────────────────────────────────────────────────────┘
```

### 9.3 数据增强流程图

```
┌─────────────────────────────────────────────────────────────┐
│              原始图像 [H×W×3] + 掩码 [H×W]                   │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  1. 随机缩放 (scale ∈ [0.75, 1.25])                         │
│     → [H'×W'×3], [H'×W']                                    │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  2. 随机裁剪/填充                                            │
│     if H'≥H and W'≥W: 随机裁剪                              │
│     else: 中心填充                                           │
│     → [H×W×3], [H×W]                                        │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  3. 水平翻转 (概率 50%)                                      │
│     → [H×W×3], [H×W]                                        │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  4. 颜色抖动 (概率 80%)                                      │
│     RGB通道独立调整                                           │
│     → [H×W×3]                                               │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  5. 亮度/对比度调整 (概率 80%)                               │
│     → [H×W×3]                                               │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  6. 高斯模糊 (概率 30%)                                      │
│     → [H×W×3]                                               │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  7. 噪声添加 (概率 25%)                                      │
│     → [H×W×3]                                               │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  最终归一化                                                  │
│  图像: [0,255] → [0,1] → Tensor [3×H×W]                    │
│  掩码: [0,255] → {0,1} → Tensor [1×H×W]                    │
└─────────────────────────────────────────────────────────────┘
```

---

## 10. 创新点与技术亮点

### 10.1 核心创新点

#### 10.1.1 多模块协同的渐进式细化架构

**创新描述**:
- **DPT Head**: 多尺度特征融合，生成初始分割结果
- **Mamba Decoder**: 轻量级状态空间模型，基于查询机制细化分割
- **UGBD Refiner**: 不确定性引导的边界细化，迭代优化边界质量
- **SDF Head**: 形状先验约束，通过几何约束优化边界

**技术亮点**:
- 渐进式细化：每个模块在前一个模块的基础上进一步优化
- 残差连接：所有细化模块都采用残差连接，保证训练稳定性
- 模块化设计：各模块可独立启用/禁用，便于消融实验

#### 10.1.2 轻量级Mamba解码器

**创新描述**:
- 基于状态空间模型的轻量级解码器
- 多查询机制：12个可学习查询向量，每个生成一个掩码
- GroupSSM：分组状态空间模块，减少参数量和计算量
- IoU权重：根据查询质量动态加权融合多个掩码

**技术亮点**:
- 参数量少：相比Transformer解码器，参数量显著减少
- 长距离依赖：状态空间模型能够有效建模长距离依赖
- 多查询融合：通过多个查询向量捕获不同尺度的特征

#### 10.1.3 不确定性引导的边界细化 (UGBD)

**创新描述**:
- 不确定性计算：`uncert = 4 × prob × (1 - prob)`
- 小波风格边缘提取：使用固定高通核提取边缘特征
- ConvGRU迭代细化：通过4步迭代逐步优化边界
- 多特征融合：图像特征、概率、边缘、不确定性融合

**技术亮点**:
- 自适应关注：不确定性高的区域获得更多关注
- 迭代优化：通过多步迭代逐步细化边界
- 边缘感知：结合小波风格边缘提取，增强边界信息

#### 10.1.4 形状先验约束 (SDF)

**创新描述**:
- 有符号距离场预测：预测每个像素到边界的距离
- 残差融合：`logits = logits - λ × SDF`
- 几何约束：Eikonal损失保证梯度模长为1
- 平滑约束：曲率损失保证SDF平滑

**技术亮点**:
- 几何先验：利用形状先验约束优化边界
- 多损失约束：L1损失、Eikonal损失、曲率损失联合优化
- 残差融合：通过减法融合，SDF值越大，logits减小越多

### 10.2 损失函数创新

#### 10.2.1 小波域损失

**创新描述**:
- **Wavelet Detail Loss**: 多尺度高频细节损失
- **Wavelet Edge Dice**: 小波域边缘Dice损失
- **TV-L1 Loss**: 总变分正则化，保证平滑性
- **Boundary Focal BCE**: 边界聚焦损失，强化边界学习

**技术亮点**:
- 多尺度分析：3层小波分解，捕获不同尺度的细节
- 高频保持：通过小波损失保持高频细节信息
- 边界强化：边界聚焦损失直接优化边界区域

#### 10.2.2 边界优化损失

**创新描述**:
- **Boundary IoU Loss**: 直接优化边界IoU，提升Fwβ指标
- **Uncertainty-Weighted Boundary Loss**: 不确定性加权边界损失
- **Fourier Magnitude Loss**: 傅里叶幅度损失，高频细节优化

**技术亮点**:
- 直接优化：Boundary IoU Loss直接优化Fwβ指标
- 自适应权重：不确定性高的区域获得更高权重
- 频域分析：通过傅里叶变换分析高频细节

#### 10.2.3 拓扑损失

**创新描述**:
- **clDice Loss**: 中心线Dice损失，保持拓扑结构
- 软骨架化：通过形态学操作提取中心线
- 拓扑精度和召回：分别计算拓扑精度和召回率

**技术亮点**:
- 拓扑保持：通过中心线Dice损失保持拓扑结构
- 软操作：使用软形态学操作，可微分

### 10.3 训练策略创新

#### 10.3.1 多损失加权策略

**创新描述**:
- 14种损失函数联合优化
- 精细的权重调优：每个损失函数都有独立的权重
- 损失函数分组：基础损失、小波损失、边界损失、拓扑损失、SDF损失

**技术亮点**:
- 全面优化：从像素级、结构级、边界级、拓扑级、几何级全面优化
- 权重平衡：通过精细的权重调优平衡不同损失函数

#### 10.3.2 数据增强策略

**创新描述**:
- 针对水下图像的特殊增强
- 颜色抖动：RGB通道独立调整，模拟水下色偏
- 亮度/对比度调整：模拟光照变化
- 模糊与噪声：模拟水下退化

**技术亮点**:
- 领域适应：针对水下图像的特殊增强策略
- 多样化：7种不同的增强策略组合

#### 10.3.3 训练技巧

**创新描述**:
- **EMA**: 指数移动平均，平滑模型参数
- **梯度裁剪**: 防止梯度爆炸
- **学习率调度**: Warmup + Cosine Annealing
- **TTA**: 测试时增强，提升评估性能

**技术亮点**:
- 稳定训练：EMA和梯度裁剪保证训练稳定性
- 性能提升：TTA显著提升评估指标

### 10.4 评估指标创新

#### 10.4.1 多指标综合评估

**创新描述**:
- **MAE**: 平均绝对误差，像素级精度
- **mIoU**: 平均IoU，重叠度评估
- **S-measure**: 结构相似性度量
- **Fwβ**: 加权F-measure，边界质量评估（主要指标）
- **E-measure**: 增强对齐度量
- **maxF**: 最大F-measure

**技术亮点**:
- 全面评估：从像素级、结构级、边界级全面评估
- 边界重点：Fwβ作为主要指标，重点关注边界质量

#### 10.4.2 TTA策略

**创新描述**:
- 多尺度测试：3个尺度 [1.0, 0.75, 1.25]
- 水平翻转：增强测试多样性
- 平均融合：6次预测的平均值

**技术亮点**:
- 性能提升：TTA显著提升评估指标
- 鲁棒性：多尺度测试提升模型鲁棒性

### 10.5 模型效率

#### 10.5.1 轻量级设计

**创新描述**:
- DINOv3 Backbone冻结：减少可训练参数
- Mamba Decoder轻量级：相比Transformer解码器参数量少
- GroupSSM分组：减少计算量
- 模块化设计：可按需启用/禁用模块

**技术亮点**:
- 高效训练：冻结Backbone，只训练解码器和细化模块
- 轻量级解码器：Mamba Decoder参数量少，计算效率高

### 10.6 论文撰写要点

#### 10.6.1 架构图绘制要点

1. **整体架构图**:
   - 清晰展示4个主要模块：DPT Head, Mamba Decoder, UGBD Refiner, SDF Head
   - 标注数据流：输入尺寸、特征维度、输出尺寸
   - 标注关键参数：查询数量、迭代步数、融合权重

2. **模块详细图**:
   - **Mamba Decoder**: 展示ImageTokenEncoder、GroupSSM、查询机制
   - **UGBD Refiner**: 展示不确定性计算、ConvGRU迭代、特征融合
   - **SDF Head**: 展示SDF预测、残差融合、损失约束

3. **损失函数图**:
   - 展示14种损失函数的分类和权重
   - 小波分解示意图
   - 边界提取示意图

#### 10.6.2 实验对比要点

1. **消融实验**:
   - 各模块的贡献（DPT, Mamba, UGBD, SDF）
   - 损失函数的贡献（基础损失、小波损失、边界损失、拓扑损失、SDF损失）
   - 关键参数的影响（查询数量、迭代步数、融合权重）

2. **对比实验**:
   - 与SOTA方法的对比（MAE, mIoU, Sα, Fwβ, mEφ, maxF）
   - 参数量和计算量的对比
   - 推理速度的对比

3. **可视化结果**:
   - 不同模块的输出可视化
   - 边界细化过程可视化
   - SDF预测可视化
   - 失败案例分析

#### 10.6.3 数学公式要点

1. **前向传播公式**:
   - DPT Head公式
   - Mamba Decoder公式
   - UGBD Refiner公式
   - SDF Head公式

2. **损失函数公式**:
   - 所有14种损失函数的数学表达
   - 总损失公式

3. **关键算法**:
   - ConvGRU迭代公式
   - SDF GT生成算法
   - 小波分解算法

---

## 11. 代码实现细节

### 11.1 关键代码位置

| 模块 | 文件 | 关键函数/类 |
|-----|------|------------|
| **DPT Head** | `dpt.py` | `DPT`, `DPTHead |
| **Mamba Decoder** | `mamba_decoder.py` | `GMSAMDecoderLite`, `GroupSSM1D`, `ImageTokenEncoder` |
| **UGBD Refiner** | `ugbd_refiner.py` | `UGBDRefiner`, `ConvGRUCell`, `wavelet_like_edges` |
| **SDF Head** | `sdf_head.py` | `SDFHead`, `batch_build_sdf`, `eikonal_loss`, `curvature_loss` |
| **损失函数** | `losses.py`, `losses_wavelet.py`, `losses_topology.py` | 各种损失函数实现 |
| **评估指标** | `metrics_eval.py` | `Evaluator`, `Fw_beta`, `S_measure`, `E_measures` |
| **训练脚本** | `train_segdinov3.py` | `train_one_epoch`, `evaluate_with_module`, `main` |
| **数据加载** | `train_segdinov3.py` | `SmartFolderDataset`, `augment_underwater` |

### 11.2 关键参数配置位置

| 参数类别 | 配置文件 | 位置 |
|---------|---------|------|
| **训练配置** | `train_aggressive.sh` | 第9-84行 |
| **模型参数** | `train_segdinov3.py` | 第315-336行 |
| **损失权重** | `train_aggressive.sh` | 第32-64行 |
| **评估配置** | `train_aggressive.sh` | 第66-70行 |

---

## 12. 性能指标 (最优版本)

### 12.1 目标指标

- **MAE**: 0.0168
- **Fwβ**: 0.9232

### 12.2 当前最佳指标

**最佳模型**: `best_Fw_beta_ep068_0.9221.pth` (Epoch 68)

- **MAE**: 0.0168
- **mIoU**: 0.8730
- **Sα**: 0.9273
- **Fwβ**: 0.9221
- **mEφ**: 0.9704
- **E_φ_adp**: 0.9732
- **maxF**: 0.9263

*结果基于完整训练配置（Wavelet损失、UGBD细化器、SDF头），训练120个epoch，使用激进优化设置。*

### 12.3 模型复杂度

- **总参数量**: ~XX M (需实际计算)
- **可训练参数量**: ~XX M (需实际计算)
- **计算量**: ~XX GFLOPs (需实际计算)

---

## 13. 总结

本文档全面梳理了SegDINOv3 + SDF + Wavelet + UGBD模型的每个细节，包括：

1. **模型架构**: 4个主要模块的详细实现
2. **数据流程**: 数据加载、预处理、增强策略
3. **损失函数**: 14种损失函数的详细说明
4. **训练流程**: 完整的训练和验证流程
5. **评估指标**: 6种评估指标的详细说明
6. **数学公式**: 所有关键公式的数学表达
7. **流程图**: 前向传播、训练、数据增强流程图
8. **创新点**: 核心创新点和技术亮点
9. **代码实现**: 关键代码位置和参数配置




