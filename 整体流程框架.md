# SegDINOv3 + SDF + Wavelet 整体流程框架

## 📋 目录
1. [项目概述](#项目概述)
2. [系统架构](#系统架构)
3. [数据流程](#数据流程)
4. [模型组件](#模型组件)
5. [训练流程](#训练流程)
6. [损失函数体系](#损失函数体系)
7. [评估指标](#评估指标)
8. [关键文件说明](#关键文件说明)

---

## 项目概述

本项目是一个基于 **DINOv3** 的图像分割框架，结合了：
- **DINOv3 Backbone**: 冻结的预训练视觉编码器
- **DPT Head**: 多尺度特征融合
- **Mamba Decoder**: 轻量级状态空间模型解码器
- **UGBD Refiner**: 不确定性引导的边界细化模块
- **SDF Head**: 形状先验（有符号距离场）头
- **Wavelet Losses**: 小波域损失函数

**目标**: 在保持高效性的同时，实现高精度的图像分割（特别是边界质量）

---

## 系统架构

```
输入图像 (B×3×H×W)
    ↓
┌─────────────────────────────────────┐
│  DINOv3 Backbone (冻结)              │
│  - 提取多尺度特征                    │
│  - 输出4层特征图                     │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│  DPT Head                            │
│  - 特征投影与融合                    │
│  - 输出初始 logits (B×1×H×W)         │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│  Mamba Decoder                       │
│  - 图像token编码                     │
│  - 查询向量交互                      │
│  - 生成细化 logits                   │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│  UGBD Refiner (可选)                 │
│  - 不确定性引导                     │
│  - ConvGRU迭代细化                   │
│  - 边界优化                          │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│  SDF Head (可选)                     │
│  - 预测有符号距离场                  │
│  - 几何先验校正                      │
│  - 残差融合                          │
└─────────────────────────────────────┘
    ↓
最终 logits (B×1×H×W) → sigmoid → 概率图
```

---

## 数据流程

### 1. 数据加载 (`SmartFolderDataset`)

**数据组织结构**:
```
segdata/
  └── {dataset_name}/
      ├── train/
      │   ├── image/  (图像文件)
      │   └── mask/   (掩码文件)
      └── test/
          ├── image/
          └── mask/
```

**数据预处理流程**:
```
原始图像 (BGR) 
  → RGB转换
  → 数据增强 (训练时):
     - 随机缩放 (0.75-1.25)
     - 随机裁剪/填充
     - 水平翻转 (50%)
     - 颜色抖动 (RGB通道独立调整)
     - 亮度/对比度调整
     - 高斯模糊 (30%)
     - 噪声添加 (25%)
  → 调整尺寸到 (H, W)
  → 归一化到 [0, 1]
  → 转换为 Tensor (3×H×W)
```

**掩码处理**:
```
灰度掩码 (0-255)
  → 二值化 (>127 → 1, else → 0)
  → 转换为 Tensor (1×H×W)
```

### 2. 数据增强策略 (`augment_underwater`)

针对水下图像的特殊增强：
- **几何变换**: 缩放、裁剪、翻转
- **颜色增强**: RGB通道独立调整（模拟水下色偏）
- **亮度对比度**: 模拟光照变化
- **模糊与噪声**: 模拟水下退化

---

## 模型组件

### 1. DINOv3 Backbone (`dpt.py`)

**功能**: 提取多尺度视觉特征

**结构**:
- 冻结的 DINOv3 ViT (small/base)
- 提取4层中间特征: `[layer_2, layer_5, layer_8, layer_11]`
- 特征维度: `[96, 192, 384, 768]` (small) 或 `[256, 512, 1024, 768]` (base)

**输出**: 4个特征图 `[B×C_i×H_i×W_i]`

### 2. DPT Head (`dpt.py`)

**功能**: 多尺度特征融合，生成初始分割结果

**结构**:
```
4层特征 → 1×1投影 → 特征融合 → 输出卷积 → logits
```

**流程**:
1. 将4层特征投影到统一维度
2. 上采样到相同尺寸
3. 拼接融合
4. 输出初始 logits (B×1×H×W)

### 3. Mamba Decoder (`mamba_decoder.py`)

**功能**: 基于状态空间模型的轻量级解码器

**核心组件**:
- **ImageTokenEncoder**: 将图像+概率编码为token序列
- **GroupSSM1D**: 分组状态空间模块（类似Mamba）
- **查询机制**: 可学习的查询向量

**流程**:
```
图像 + 概率图 → Token编码 (B×L×C)
    ↓
查询向量 (B×N×C) + Token交互
    ↓
GroupSSM处理 (多层)
    ↓
生成掩码系数 → 加权融合 → 残差输出
```

**特点**:
- 轻量级（参数量少）
- 长距离依赖建模
- 多查询融合机制

### 4. UGBD Refiner (`ugbd_refiner.py`)

**功能**: 不确定性引导的边界细化

**输入**: 
- 图像 (B×3×H×W)
- logits (B×1×H×W)

**处理流程**:
```
1. 计算不确定性: uncert = 4 × prob × (1-prob)
2. 提取边缘特征: wavelet_like_edges(image)
3. 特征融合: [img_feat, prob, edge, uncert]
4. ConvGRU迭代细化 (steps=3-4)
5. 残差输出: logits + refinement
```

**关键特性**:
- **不确定性引导**: 关注低置信度区域
- **迭代细化**: ConvGRU多步迭代
- **边缘感知**: 小波风格边缘提取

### 5. SDF Head (`sdf_head.py`)

**功能**: 形状先验约束，通过有符号距离场优化边界

**输入**:
- 图像 (B×3×H×W)
- logits (B×1×H×W)

**处理流程**:
```
1. 提取边缘: wavelet_like_edges(image)
2. 特征拼接: [image, prob, edge] → (B×5×H×W)
3. 编码器: 3层卷积 → 特征 (B×C×H×W)
4. 预测SDF: (B×1×H×W)
5. 残差融合: refined_logits = logits - λ × sdf
```

**损失函数**:
- **L1损失**: `L1(SDF_pred, SDF_gt)`
- **Eikonal损失**: `||∇SDF|| - 1` (保证梯度模长为1)
- **曲率损失**: `||ΔSDF||²` (平滑约束)

---

## 训练流程

### 主训练脚本: `train_segdinov3.py`

### 训练循环结构

```
初始化:
  ├── 设置随机种子
  ├── 加载DINOv3预训练权重
  ├── 构建模型组件 (backbone, decoder, refiner, sdf_head)
  ├── 初始化优化器 (AdamW)
  ├── 构建学习率调度器 (Cosine + Warmup)
  ├── 可选: EMA (指数移动平均)
  └── 加载数据集

训练循环 (epochs):
  ├── 训练阶段 (train_one_epoch):
  │   ├── 前向传播:
  │   │   ├── backbone → DPT → decoder
  │   │   ├── (可选) UGBD refiner
  │   │   └── (可选) SDF head
  │   ├── 计算损失 (多损失加权和)
  │   ├── 反向传播
  │   ├── 梯度裁剪 (可选)
  │   └── 优化器更新
  │
  ├── 验证阶段 (evaluate_with_module):
  │   ├── 模型评估模式
  │   ├── (可选) TTA (测试时增强)
  │   ├── 计算评估指标
  │   └── 保存可视化结果 (可选)
  │
  ├── 保存检查点:
  │   ├── latest.pth (最新)
  │   └── best_{metric}_ep{epoch}_{value}.pth (最佳)
  │
  ├── 记录指标到CSV
  └── 更新学习率

总结:
  └── 输出最佳指标统计
```

### 关键训练参数

**优化器**:
- 类型: AdamW
- 学习率: 6e-5 ~ 1e-4
- Weight Decay: 1.2e-4 ~ 1.5e-4

**学习率调度**:
- Warmup: 8-12 epochs
- 主调度: Cosine Annealing
- 总epochs: 80-120

**正则化**:
- 梯度裁剪: 0.8-1.0
- EMA (可选): decay=0.996-0.999

---

## 损失函数体系

### 1. 基础损失

| 损失函数 | 权重 | 说明 |
|---------|------|------|
| **BCE** | 1.0 | 二元交叉熵，基础分类损失 |
| **IoU/Dice** | 0.5-0.7 | 重叠度损失，提升整体精度 |
| **SSIM** | 0.35-0.45 | 结构相似性，保持结构一致性 |

### 2. 小波域损失 (`losses_wavelet.py`)

| 损失函数 | 权重 | 说明 |
|---------|------|------|
| **Wavelet Detail** | 0.30-0.35 | 小波细节损失，保持高频信息 |
| **Wavelet Edge Dice** | 0.10-0.15 | 小波边缘Dice损失 |
| **TV L1** | 0.06-0.10 | 总变分正则化，平滑约束 |
| **Boundary Focal BCE** | 0.12-0.18 | 边界聚焦损失，强化边界学习 |

### 3. 边界优化损失 (`losses.py`)

| 损失函数 | 权重 | 说明 |
|---------|------|------|
| **Boundary IoU** | 0.20-0.25 | 边界IoU损失，直接优化Fwβ |
| **Uncertainty-Weighted Boundary** | 0.15-0.20 | 不确定性加权边界损失 |
| **Fourier Magnitude** | 0.08-0.10 | 傅里叶幅度损失，高频细节 |

### 4. 拓扑损失 (`losses_topology.py`)

| 损失函数 | 权重 | 说明 |
|---------|------|------|
| **clDice** | 0.06-0.08 | 中心线Dice，保持拓扑结构 |

### 5. SDF形状先验损失 (`sdf_head.py`)

| 损失函数 | 权重 | 说明 |
|---------|------|------|
| **SDF L1** | 0.12-0.15 | SDF预测与GT的L1损失 |
| **Eikonal** | 0.06-0.08 | 梯度模长约束 |
| **Curvature** | 0.02-0.04 | 曲率平滑约束 |

### 总损失公式

```
L_total = w_bce × L_BCE
        + w_iou × L_IoU
        + w_ssim × L_SSIM
        + w_wav × L_wavelet_detail
        + w_wed × L_wavelet_edge_dice
        + w_tv × L_TV
        + w_bfce × L_boundary_focal
        + w_biou × L_boundary_iou
        + w_uwb × L_uncertainty_weighted_boundary
        + w_freq × L_fourier
        + w_cldice × L_clDice
        + w_sdf × L_SDF_L1
        + w_eik × L_Eikonal
        + w_curv × L_Curvature
```

---

## 评估指标

### 评估器: `Evaluator` (`metrics_eval.py`)

### 核心指标

| 指标 | 说明 | 范围 | 越高越好 |
|-----|------|------|---------|
| **MAE** | 平均绝对误差 | [0, 1] | ❌ |
| **mIoU** | 平均IoU | [0, 1] | ✅ |
| **S_alpha (S-measure)** | 结构相似性度量 | [0, 1] | ✅ |
| **Fw_beta** | 加权F-measure (β=0.3) | [0, 1] | ✅ |
| **mE_phi** | 平均E-measure | [0, 1] | ✅ |
| **E_phi_adp** | 自适应E-measure | [0, 1] | ✅ |
| **maxF** | 最大F-measure | [0, 1] | ✅ |

### 评估流程

```
评估模式:
  ├── 模型设置为eval()
  ├── 遍历验证集:
  │   ├── (可选) TTA前向传播
  │   ├── 计算logits
  │   ├── (可选) Refiner处理
  │   ├── (可选) SDF Head处理
  │   ├── sigmoid → 概率图
  │   └── 转换为uint8格式
  │
  ├── 使用PySODMetrics计算指标:
  │   ├── MAE
  │   ├── S-measure
  │   ├── E-measure
  │   ├── F-measure (加权)
  │   └── IoU
  │
  └── 返回指标字典
```

### TTA (Test Time Augmentation)

**策略**:
- **多尺度**: [1.0, 0.75, 1.25]
- **水平翻转**: 是/否
- **融合方式**: 平均

**流程**:
```
原始图像
  ├── 尺度1.0 → 预测 → 反变换
  ├── 尺度0.75 → 预测 → 反变换
  ├── 尺度1.25 → 预测 → 反变换
  └── 每个尺度 + 翻转 → 平均
```

---

## 关键文件说明

### 核心训练文件

| 文件 | 功能 |
|-----|------|
| `train_segdinov3.py` | 主训练脚本，包含完整训练流程 |
| `train_aggressive.sh` | 激进优化训练配置脚本 |
| `train_optimized.sh` | 优化训练配置脚本 |

### 模型组件

| 文件 | 功能 |
|-----|------|
| `dpt.py` | DPT Head实现，多尺度特征融合 |
| `mamba_decoder.py` | Mamba解码器，轻量级状态空间模型 |
| `ugbd_refiner.py` | UGBD细化器，不确定性引导边界优化 |
| `sdf_head.py` | SDF形状先验头，有符号距离场预测 |

### 损失函数

| 文件 | 功能 |
|-----|------|
| `losses.py` | 基础损失 + 边界损失 + 傅里叶损失 |
| `losses_wavelet.py` | 小波域损失函数 |
| `losses_topology.py` | 拓扑损失（clDice） |

### 评估与指标

| 文件 | 功能 |
|-----|------|
| `metrics_eval.py` | 评估器实现，支持多种指标 |
| `metrics_maxf.py` | maxF指标计算 |
| `eval_py_sod_metrics.py` | PySODMetrics评估脚本 |

### 数据与工具

| 文件 | 功能 |
|-----|------|
| `dataset.py` | 数据集加载与预处理 |
| `blocks.py` | 基础网络块 |
| `adapters.py` | 适配器模块 |
| `ffr_module.py` | 特征融合模块 |

### 文档

| 文件 | 功能 |
|-----|------|
| `README.md` | 项目说明 |
| `EVALUATION_METRICS_DOC.md` | 评估指标详细文档 |
| `EVALUATION_QUICK_REFERENCE.md` | 评估指标快速参考 |
| `TRAINING_COMMAND.md` | 训练命令说明 |
| `OPTIMIZATION_GUIDE.md` | 优化指南 |
| `AGGRESSIVE_OPTIMIZATION.md` | 激进优化说明 |

---

## 典型训练命令

### 基础训练

```bash
python train_segdinov3.py \
  --data_dir ./segdata \
  --dataset usod10k \
  --dino_ckpt ./web_pth/dinov3_vits16_pretrain_lvd1689m-08c60483.pth \
  --dino_size s \
  --input_h 384 --input_w 384 \
  --batch_size 8 \
  --epochs 80 \
  --lr 1e-4
```

### 完整功能训练（使用所有模块）

```bash
bash train_aggressive.sh
```

或手动配置：

```bash
python train_segdinov3.py \
  --data_dir ./segdata \
  --dataset usod10k \
  --dino_ckpt ./web_pth/dinov3_vits16_pretrain_lvd1689m-08c60483.pth \
  --dino_size s \
  --input_h 384 --input_w 384 \
  --batch_size 8 \
  --epochs 120 \
  --use_wavelet \
  --use_ugbd \
  --use_sdf \
  --use_aug \
  --eval_tta \
  --use_ema \
  --lr 6e-5 \
  --w_iou 0.7 \
  --w_ssim 0.45 \
  --w_wav 0.35 \
  --w_wed 0.15 \
  --w_tv 0.10 \
  --w_bfce 0.18 \
  --w_biou 0.25 \
  --w_uwb 0.20 \
  --w_freq 0.10 \
  --w_cldice 0.08 \
  --w_sdf 0.15 \
  --w_eik 0.08 \
  --w_curv 0.04
```

---

## 工作流程总结

### 训练阶段

1. **数据准备**: 加载图像和掩码，应用数据增强
2. **特征提取**: DINOv3 backbone提取多尺度特征
3. **初始分割**: DPT Head生成初始logits
4. **解码细化**: Mamba Decoder进一步细化
5. **边界优化**: UGBD Refiner迭代优化边界
6. **形状约束**: SDF Head应用几何先验
7. **损失计算**: 多损失加权求和
8. **反向传播**: 更新模型参数

### 评估阶段

1. **模型评估模式**: 关闭dropout和batch norm更新
2. **TTA增强** (可选): 多尺度+翻转测试
3. **前向传播**: 完整模型推理
4. **指标计算**: 使用PySODMetrics计算多种指标
5. **结果保存**: 保存最佳模型和指标历史

---

## 性能优化要点

1. **边界质量**: 通过多种边界损失（Boundary IoU, UWB, Fourier）直接优化Fwβ
2. **高频细节**: 小波损失保持细节信息
3. **形状先验**: SDF约束保证几何合理性
4. **不确定性引导**: UGBD关注低置信度区域
5. **多尺度融合**: DPT和Mamba的多尺度处理
6. **迭代细化**: UGBD和SDF的迭代优化机制

---

## 注意事项

1. **依赖安装**: 需要安装 `scipy` (SDF计算) 和 `pysodmetrics` (评估)
2. **内存管理**: 大batch size可能需要调整
3. **训练时间**: 完整训练（120 epochs）可能需要较长时间
4. **超参数调优**: 根据数据集特点调整损失权重
5. **TTA评估**: 评估时使用TTA会显著提升指标，但增加计算时间

---

**最后更新**: 2025年
**版本**: SegDINOv3 + SDF + Wavelet


